---
id: 672d49c4e899345f5b33c24c
title: What Is console.log Used For, and How Does It Work?
challengeType: 19
dashedName: what-is-console-log
---

# --description--
你可以使用 `console.log()` 将文本或变量的值输出到控制台，以确认代码是否按预期运行。

`console.log()` 在 JavaScript 中做什么？

## --answers--

它用于记录数据并在浏览器控制台显示输出。

---

它会审计你的代码并将结果显示在浏览器控制台中。

### --feedback--

想想你在哪里可以看到 `console.log()` 的输出结果。

---

它会将值存入数据库并同时显示在控制台。

### --feedback--

想想你在哪里可以看到 `console.log()` 的输出结果。

---

它会更改页面上的 HTML 内容并在控制台显示这些更改。

### --feedback--

想想你在哪里可以看到 `console.log()` 的输出结果。

## --answers--

It audits your code for errors and displays the results in the browser console.

### --feedback--

Think about where you can see the results of the `console.log()` method.

---

It is used to log data and display the output in the browser console.

---

It stores values in a database as well as the browser console.

### --feedback--

Think about where you can see the results of the `console.log()` method.

---

It changes the HTML content on the page and shows the changes in the browser console.

### --feedback--

Think about where you can see the results of the `console.log()` method.

## --video-solution--

2

## --text--

下面代码会在控制台打印什么？

```js
const age = 10;
console.log(age);
```

## --answers--

`10`

---

`"10"`

### --feedback--

请记住 `console.log()` 会打印变量的值。

---

`age`

### --feedback--

请记住 `console.log()` 会打印变量的值。

---

`"age"`

### --feedback--

请记住 `console.log()` 会打印变量的值。

## --video-solution--

1

## --text--
## --answers--

为什么在构建 Web 应用时 `console.log()` 很有用？

## --answers--

它常被开发者用于调试，在开发过程中检查并查看代码中值或表达式的输出。

---

它通常用于检测应用性能并在控制台查看结果。

### --feedback--

回顾本讲开头的相关讨论。

---

它常用于检查代码的 lint 错误并在控制台显示这些错误。

### --feedback--

回顾本讲开头的相关讨论。

---

它用于确保你的 JavaScript 代码遵循最佳实践。

### --feedback--

回顾本讲开头的相关讨论。

## --video-solution--

2
