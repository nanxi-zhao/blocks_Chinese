---
id: 672a5507d857a891139abc7f
title: 什么是 aria-controls 属性，它是如何工作的？
challengeType: 19
dashedName: what-is-the-aria-controls-attribute
---

# --description--

`aria-controls` 属性用于在控制页面上另一个元素存在的元素与它控制的元素之间创建程序化关系。这种关系可以帮助屏幕阅读器用户更好地理解控件的工作原理。常见用途包括一组控制不同内容部分可见性的选项卡，或切换菜单可见性的按钮。

让我们看一个示例来了解它是如何工作的。在这个选项卡界面示例中，我们有一个带有三个按钮的 `div` 元素：

```html
<div role="tablist">
  <button role="tab" id="tab1" aria-controls="section1" aria-selected="true">
    选项卡 1
  </button>
  <button role="tab" id="tab2" aria-controls="section2" aria-selected="false">
    选项卡 2
  </button>
  <button role="tab" id="tab3" aria-controls="section3" aria-selected="false">
    选项卡 3
  </button>
</div>
```

`div` 使用 `role="tablist"` 来指示它作为一组选项卡的容器元素。

每个按钮代表一个选项卡，并且有一个设置为 `tab` 的 `role` 属性。在大多数选项卡界面中，第一个选项卡面板默认可见，因此第一个选项卡按钮有一个设置为 `true` 的 `aria-selected` 属性，以指示其关联的内容部分当前可见。`aria-controls` 属性用于将每个按钮与其控制的内容部分关联。

以下是与选项卡对应的内容部分：

```html
<div id="section1" role="tabpanel" aria-labelledby="tab1">
  第 1 部分内容
</div>

<div id="section2" role="tabpanel" aria-labelledby="tab2" hidden>
  第 2 部分内容
</div>

<div id="section3" role="tabpanel" aria-labelledby="tab3" hidden>
  第 3 部分内容
</div>
```

每个内容部分都有一个设置为 `tabpanel` 的 `role` 属性和一个指向相应选项卡的 `aria-labelledby` 属性，以为面板提供可访问的名称。`hidden` 属性用于隐藏未选择的内容部分。每个部分 ID 与相应按钮上的 `aria-controls` 属性值匹配。`section1` ID 与第一个按钮上的 `aria-controls` 属性匹配，`section2` 与第二个按钮上的 `aria-controls` 属性匹配，`section3` 与第三个按钮上的 `aria-controls` 属性匹配。这就是如何建立选项卡与其内容部分之间的关联。

要在不同部分之间切换，你需要使用 JavaScript 根据当前可见的部分更新部分上的 `hidden` 属性和按钮上的 `aria-selected` 属性。一次只能有一个部分可见，它不应有 `hidden` 属性，并且其按钮上的 `aria-selected` 应设置为 `true`。其余隐藏部分都应有 `hidden` 属性，并且它们按钮上的 `aria-selected` 应设置为 `false`。

选项卡是 `aria-controls` 属性的常见用例，但它可以用于其他场景，其中一个元素控制另一个元素的可见性或行为。其他示例包括手风琴、下拉菜单和模态框。

下次你设计涉及控制元素可见性的用户界面时，考虑使用 `aria-controls` 属性来建立控制元素和被控制元素之间的关系。

# --questions--

## --text--

在选项卡的上下文中，`aria-controls` 属性的目的是什么？

## --answers--

指定哪个选项卡当前处于活动状态。

### --feedback--

想想选项卡和内容部分之间的关联。

---

隐藏或显示内容部分。

### --feedback--

想想选项卡和内容部分之间的关联。

---

将选项卡与其控制的部分关联。

---

设置元素的角色。

### --feedback--

想想选项卡和内容部分之间的关联。

## --video-solution--

3

## --text--

`aria-controls` 属性如何影响示例中的内容部分？

## --answers--

它根据用户输入自动更新内容。

### --feedback--

想想如何使用该属性来控制内容部分的可见性。

---

它根据选择的选项卡隐藏或显示部分。

---

它更改选项卡的样式以指示它被选中。

### --feedback--

想想如何使用该属性来控制内容部分的可见性。

---

它定义选项卡的视觉外观。

### --feedback--

想想如何使用该属性来控制内容部分的可见性。

## --video-solution--

2

## --text--

在提供的示例中，如何使用 `aria-labelledby` 属性？

## --answers--

指定哪个选项卡控制哪个内容部分。

### --feedback--

它将选项卡链接到它控制的内容部分。

---

标识选项卡控制的内容部分。

---

定义选项卡和内容部分的角色。

### --feedback--

它将选项卡链接到它控制的内容部分。

---

在点击选项卡时自动在内容部分之间切换。

### --feedback--

它将选项卡链接到它控制的内容部分。

## --video-solution--

2