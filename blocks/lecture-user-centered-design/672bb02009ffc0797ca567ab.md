---
id: 672bb02009ffc0797ca567ab
title: 在表单、注册和设置中进行进度指示的最佳实践是什么？
challengeType: 19
dashedName: what-are-best-practices-for-progress-indication-on-forms-registration-and-setup
---

# --interactive--

**注意**：一些交互式示例可能使用了你尚未学习的CSS和JavaScript。不用担心理解所有代码。示例的目标是向你展示这些设计概念的预览，以便你更好地理解工作原理。

进度指示是一种向用户显示他们在流程中位置的方式。它可以用于表单、注册和设置流程中。目标是帮助用户了解他们在流程中的位置以及还需要做多少。

例如，你可以使用进度指示条向用户显示填写表单时还剩下什么。你不想创建一种情况，即用户需要填写一个冗长的表单，但他们不知道还需要完成多少步骤。透明度是关键，这样用户就知道他们是否有足够的时间坐下来完成表单，或者是否需要稍后回来。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<form id="multiStepForm">
  <div class="form-progress">
    <label class="progress-label">表单进度</label>
    <div class="progress-container">
      <div class="progress-bar"></div>
      <div class="progress-text">第1步，共3步</div>
    </div>
  </div>

  <!-- 第1步 -->
  <fieldset class="form-step active">
    <legend>个人信息</legend>
    <label for="name">全名:</label>
    <input type="text" id="name" name="name" required>

    <label for="email">邮箱:</label>
    <input type="email" id="email" name="email" required>

    <button type="button" class="next-btn">下一步</button>
  </fieldset>

  <!-- 第2步 -->
  <fieldset class="form-step">
    <legend>地址</legend>
    <label for="address">街道地址:</label>
    <input type="text" id="address" name="address" required>

    <label for="city">城市:</label>
    <input type="text" id="city" name="city" required>

    <button type="button" class="prev-btn">上一步</button>
    <button type="button" class="next-btn">下一步</button>
  </fieldset>

  <!-- 第3步 -->
  <fieldset class="form-step">
    <legend>审核与提交</legend>
    <p>请在提交前审核您的信息。</p>

    <button type="button" class="prev-btn">上一步</button>
    <button type="submit">提交</button>
  </fieldset>
</form>
<script src="index.js"></script>

```

```css
.form-progress {
  max-width: 500px;
  margin: 20px auto 30px;
  font-family: Arial, sans-serif;
}

.progress-label {
  display: block;
  margin-bottom: 8px;
  font-size: 16px;
  font-weight: 600;
  color: #333;
}

.progress-container {
  position: relative;
  background-color: #555; 
  border-radius: 8px;
  height: 30px;
  overflow: hidden;
  box-shadow: inset 0 1px 3px rgba(0,0,0,0.3);
}

.progress-bar {
  background-color: #4caf50;
  height: 100%;
  width: 0; 
  border-radius: 8px 0 0 8px;
  transition: width 0.3s ease;
}

.progress-text {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 30px;
  line-height: 30px;
  text-align: center;
  font-weight: bold;
  color: #fff;
  pointer-events: none;
  user-select: none;
}

form {
  max-width: 500px;
  margin: 0 auto;
  font-family: Arial, sans-serif;
}

fieldset {
  border: none;
  padding: 0;
  margin: 0 0 20px;
}

legend {
  font-size: 1.2em;
  font-weight: 700;
  margin-bottom: 10px;
  color: #222;
}

label {
  display: block;
  margin-bottom: 6px;
  font-weight: 600;
  color: #333;
}

input[type="text"],
input[type="email"] {
  width: 100%;
  padding: 8px 10px;
  font-size: 1em;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-bottom: 15px;
  box-sizing: border-box;
  transition: border-color 0.2s ease;
}

input[type="text"]:focus,
input[type="email"]:focus {
  outline: none;
  border-color: #4caf50;
  box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
}

.form-step {
  display: none;
}

.form-step.active {
  display: block;
}

button {
  background-color: #4caf50;
  border: none;
  color: white;
  padding: 10px 18px;
  font-size: 1em;
  border-radius: 5px;
  cursor: pointer;
  margin-right: 10px;
  transition: background-color 0.2s ease;
}

button:hover:not(:disabled) {
  background-color: #45a049;
}

button:disabled {
  background-color: #9e9e9e;
  cursor: not-allowed;
}

@media (max-width: 600px) {
  .form-progress, form {
    max-width: 90%;
    margin: 20px auto;
  }
}
```

```js
const form = document.getElementById('multiStepForm');
const steps = form.querySelectorAll('.form-step');
const progressBar = form.querySelector('.progress-bar');
const progressText = form.querySelector('.progress-text');
const totalSteps = steps.length;

let currentStep = 0;

function updateProgress() {
  const percent = ((currentStep + 1) / totalSteps) * 100;
  progressBar.style.width = percent + '%';
  progressText.textContent = `第${currentStep + 1}步，共${totalSteps}步`;
}

function showStep(index) {
  steps.forEach((step, i) => {
    step.classList.toggle('active', i === index);
  });
  updateProgress();
}

form.querySelectorAll('.next-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    if (currentStep < totalSteps - 1) {
      currentStep++;
      showStep(currentStep);
    }
  });
});

form.querySelectorAll('.prev-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    if (currentStep > 0) {
      currentStep--;
      showStep(currentStep);
    }
  });
});

showStep(currentStep);

form.addEventListener('submit', e => {
  e.preventDefault();
  alert('表单已提交！');
});

```

:::

在设计进度指示部分时，有几个最佳实践需要牢记。第一个考虑因素是保持简洁。你不想用太多信息让用户感到不知所措，以至于他们感到沮丧并离开网站。

第二个考虑因素是让用户能够回到之前的步骤。这很重要，因为用户可能想要回去检查他们的之前答案或进行修改。

另一个考虑因素是让进度指示部分易于找到。如果用户找不到它，他们就不会知道他们在流程中的进度。

最后一个考虑因素是使用清晰的章节标题、百分比或步骤。如果你只有一个没有上下文的进度条，用户就不会知道它的含义。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<form id="progressForm">
  <div class="form-progress" aria-label="表单进度">
    <label class="progress-label">第<span id="currentStep">1</span>步，共<span id="totalSteps">4</span>步 (<span id="percentage">25%</span>)</label>
    <div class="progress-container" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="25">
      <div class="progress-bar"></div>
    </div>
  </div>

  <!-- 第1步 -->
  <fieldset class="form-step active">
    <legend>基本信息</legend>
    <p>请输入您的基本信息。</p>
    <label for="name">姓名:</label>
    <input type="text" id="name" name="name" required>

    <button type="button" class="next-btn">下一步</button>
  </fieldset>

  <!-- 第2步 -->
  <fieldset class="form-step">
    <legend>联系详情</legend>
    <p>我们如何联系您？</p>
    <label for="email">邮箱:</label>
    <input type="email" id="email" name="email" required>

    <button type="button" class="prev-btn">上一步</button>
    <button type="button" class="next-btn">下一步</button>
  </fieldset>

  <!-- 第3步 -->
  <fieldset class="form-step">
    <legend>地址</legend>
    <p>您住在哪里？</p>
    <label for="address">街道:</label>
    <input type="text" id="address" name="address" required>

    <button type="button" class="prev-btn">上一步</button>
    <button type="button" class="next-btn">下一步</button>
  </fieldset>

  <!-- 第4步 -->
  <fieldset class="form-step">
    <legend>审核</legend>
    <p>在提交前审核您的答案。</p>

    <button type="button" class="prev-btn">上一步</button>
    <button type="submit">提交</button>
  </fieldset>
</form>
<script src="index.js"></script>
```

```css
.form-progress {
  max-width: 500px;
  margin: 20px auto 30px;
  font-family: Arial, sans-serif;
}

.progress-label {
  display: block;
  margin-bottom: 8px;
  font-size: 16px;
  font-weight: 600;
  color: #333;
}

.progress-container {
  position: relative;
  background-color: #555;
  border-radius: 8px;
  height: 30px;
  overflow: hidden;
  box-shadow: inset 0 1px 3px rgba(0,0,0,0.3);
}

.progress-bar {
  background-color: #4caf50;
  height: 100%;
  width: 0;
  border-radius: 8px 0 0 8px;
  transition: width 0.3s ease;
}

.progress-text {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 30px;
  line-height: 30px;
  text-align: center;
  font-weight: bold;
  color: #fff;
  pointer-events: none;
  user-select: none;
}

form {
  max-width: 500px;
  margin: 0 auto;
  font-family: Arial, sans-serif;
}

fieldset {
  border: none;
  padding: 0;
  margin: 0 0 20px;
}

legend {
  font-size: 1.2em;
  font-weight: 700;
  margin-bottom: 10px;
  color: #222;
}

label {
  display: block;
  margin-bottom: 6px;
  font-weight: 600;
  color: #333;
}

input[type="text"],
input[type="email"] {
  width: 100%;
  padding: 8px 10px;
  font-size: 1em;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-bottom: 15px;
  box-sizing: border-box;
  transition: border-color 0.2s ease;
}

input[type="text"]:focus,
input[type="email"]:focus {
  outline: none;
  border-color: #4caf50;
  box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
}

.form-step {
  display: none;
}

.form-step.active {
  display: block;
}

button {
  background-color: #4caf50;
  border: none;
  color: white;
  padding: 10px 18px;
  font-size: 1em;
  border-radius: 5px;
  cursor: pointer;
  margin-right: 10px;
  transition: background-color 0.2s ease;
}

button:hover:not(:disabled) {
  background-color: #45a049;
}

button:disabled {
  background-color: #9e9e9e;
  cursor: not-allowed;
}

@media (max-width: 600px) {
  .form-progress, form {
    max-width: 90%;
    margin: 20px auto;
  }
}
```

```js
const form = document.getElementById('progressForm');
const steps = form.querySelectorAll('.form-step');
const progressBar = form.querySelector('.progress-bar');
const currentStepSpan = document.getElementById('currentStep');
const totalStepsSpan = document.getElementById('totalSteps');
const percentageSpan = document.getElementById('percentage');

const totalSteps = steps.length;
let currentStep = 0;

totalStepsSpan.textContent = totalSteps;

function updateProgress() {
  const percent = Math.round(((currentStep + 1) / totalSteps) * 100);
  progressBar.style.width = percent + '%';
  currentStepSpan.textContent = currentStep + 1;
  percentageSpan.textContent = percent + '%';

  // 更新ARIA属性
  form.querySelector('.progress-container').setAttribute('aria-valuenow', percent);
}

function showStep(index) {
  steps.forEach((step, i) => {
    step.classList.toggle('active', i === index);
  });
  updateProgress();
}

form.querySelectorAll('.next-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    if (currentStep < totalSteps - 1) {
      currentStep++;
      showStep(currentStep);
    }
  });
});

form.querySelectorAll('.prev-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    if (currentStep > 0) {
      currentStep--;
      showStep(currentStep);
    }
  });
});

form.addEventListener('submit', e => {
  e.preventDefault();
  alert('表单已提交！谢谢！');
});

showStep(currentStep);

```

:::

这些只是在表单、注册和设置流程中设计进度指示时需要牢记的一些最佳实践。研究一些大网站上的示例，看看它们如何实现进度指示。然后用真实用户测试你的设计，看看它的工作效果如何。

# --questions--

## --text--

在表单或注册流程中使用进度指示的主要目标是什么？

## --answers--

在用户完成表单时娱乐他们。

### --feedback--

专注于向用户提供有关他们进度的信息。

---

告知用户他们在流程中的位置以及还剩下什么要做。

---

减少流程中的步骤数。

### --feedback--

专注于向用户提供有关他们进度的信息。

---

加快表单提交流程。

### --feedback--

专注于向用户提供有关他们进度的信息。

## --video-solution--

2

## --text--

为什么允许用户回到表单或注册流程的之前步骤很重要？

## --answers--

让用户更改答案或查看之前的条目。

---

让表单更复杂。

### --feedback--

想想用户为什么可能需要重新访问流程的早期部分。

---

限制流程中的步骤数。

### --feedback--

想想用户为什么可能需要重新访问流程的早期部分。

---

增加用户在网站上花费的时间。

### --feedback--

想想用户为什么可能需要重新访问流程的早期部分。

## --video-solution--

1

## --text--

在网页上放置进度指示部分时应该考虑什么？

## --answers--

它应该尽可能小以避免分散用户的注意力。

### --feedback--

考虑用户将如何与进度指示交互和理解它。

---

它应该易于找到，并用清晰的上下文标记，如章节标题或百分比。

---

它应该隐藏直到用户到达最后一步。

### --feedback--

考虑用户将如何与进度指示交互和理解它。

---

它应该只包含一个没有文本的视觉进度条。

### --feedback--

考虑用户将如何与进度指示交互和理解它。

## --video-solution--

2