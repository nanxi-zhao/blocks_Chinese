
# --questions--

## --text--

以下哪一项关于 JavaScript 数组是不正确的？

## --answers--

它们使用从 0 开始的索引。

### --feedback--

回想数组中每个值是如何用数字索引的，以及这会如何影响值的存储方式。

---

它们是一个无序的值集合。

---

它们可以包含另一个数组作为值。

### --feedback--

回想数组中每个值是如何用数字索引的，以及这会如何影响值的存储方式。

---

它们是动态的，创建后大小可以改变。

### --feedback--

回想数组中每个值是如何用数字索引的，以及这会如何影响值的存储方式。

## --video-solution--

2

## --text--

下面这段代码将输出什么？

```js
let numbers = [1, 2, 3, 4, 5];
console.log(numbers[2]);
```

## --answers--

`2`

### --feedback--

记住 JavaScript 的数组使用从 0 开始的索引。

---

`3`

---

`4`

### --feedback--

记住 JavaScript 的数组使用从 0 开始的索引。

---

`5`

### --feedback--

记住 JavaScript 的数组使用从 0 开始的索引。

## --video-solution--

2

## --text--

下面这段代码将输出什么？

```js
let colors = ["red", "green", "blue"];
console.log(colors.length);
```

## --answers--

`2`

### --feedback--

`length` 属性返回数组中包含的元素数量。

---

`3`

---

`4`

### --feedback--

`length` 属性返回数组中包含的元素数量。

---

`undefined`

### --feedback--

`length` 属性返回数组中包含的元素数量。

## --video-solution--

2
### --feedback--

Remember that arrays in JavaScript are zero-indexed.

## --video-solution--

2

## --text--

What will be the output of the following code?

```js
let colors = ["red", "green", "blue"];
console.log(colors.length);
```

## --answers--

`2`

### --feedback--

The length property of an array returns the number of elements it contains.

---

`3`

---

`4`

### --feedback--

The length property of an array returns the number of elements it contains.

---

`undefined`

### --feedback--

The length property of an array returns the number of elements it contains.

## --video-solution--

2
