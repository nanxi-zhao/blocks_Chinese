---
id: 6839e4b003571c149bcda122
title: What Are the Enumerate and Zip Functions and How Do They Work?
challengeType: 19
dashedName: what-are-the-enumerate-and-zip-functions-and-how-do-they-work
---

# --description--
# --description--

前面我们学习了 `for` 循环，它可以遍历列表并对每个元素执行操作。如果想同时跟踪元素的索引，可以使用 `enumerate()`，它会为可迭代对象返回一个包含 (index, value) 的枚举对象。

例如把 `enumerate()` 转为列表会得到：

```py
languages = ['Spanish', 'English', 'Russian', 'Chinese']
list(enumerate(languages))
# [(0, 'Spanish'), (1, 'English'), (2, 'Russian'), (3, 'Chinese')]
```

在 `for` 循环中解包后，可以直接使用索引和元素：

```py
for index, language in enumerate(languages):
    print(f'Index {index} and language {language}')
```

`enumerate()` 还接受可选的 `start` 参数，用于指定计数起始值：

```py
for index, language in enumerate(languages, 1):
    print(f'Index {index} and language {language}')
```

若需要并行遍历多个可迭代对象，可以使用 `zip()`，它会把多个序列按位置配对并返回元组的迭代器：

```py
developers = ['Naomi', 'Dario', 'Jessica', 'Tom']
ids = [1, 2, 3, 4]
list(zip(developers, ids))
# [('Naomi', 1), ('Dario', 2), ('Jessica', 3), ('Tom', 4)]
```

结合 `for` 循环使用时，你可以解包每对元素：

```py
for name, id in zip(developers, ids):
    print(f'Name: {name}')
    print(f'ID: {id}')
```

`enumerate()` 与 `zip()` 都能让循环逻辑更简洁高效，建议在遍历场景中优先考虑使用它们。

# --questions--

## --text--

What does the `enumerate()` function do?

## --answers--

It is used to print the memory addresses for each element in a list.
## --video-solution--

3

## --text--

`enumerate()` 函数有什么作用？

## --answers--

用于打印列表中每个元素的内存地址。

### --feedback--

请复习本节课开头寻找答案。

---

用于把列表转换成元组或集合并返回一个 `enumerate` 对象。

### --feedback--

请复习本节课开头寻找答案。

---

用于跟踪可迭代对象中元素的索引并返回一个 `enumerate` 对象。

---

用于加速 Python 应用程序的性能。

### --feedback--

请复习本节课开头寻找答案。

## --video-solution--

3

## --text--

在 `enumerate()` 函数中，下面哪个可选参数用于指定计数的起始值？

## --answers--

`set`

### --feedback--

复习本节课结尾部分来找出答案。

---

`position`

### --feedback--

复习本节课结尾部分来找出答案。

---

`start`

---

`count`

### --feedback--

复习本节课结尾部分来找出答案。

## --video-solution--

3

## --text--

`zip()` 函数有什么作用？

## --answers--

用于并行遍历多个可迭代对象。

---

用于创建压缩（zip）文件。

### --feedback--

复习本节课结尾部分来找出答案。

---

用于跳出嵌套循环。

### --feedback--

复习本节课结尾部分来找出答案。

---

用于从列表创建一种节省内存的可迭代对象。

### --feedback--

复习本节课结尾部分来找出答案。

## --video-solution--

1
