---
id: 66edd07682767adff3a6231e
title: JavaScript DOM操作和点击事件测验
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是文档对象模型？

#### --distractors--

用于与HTML页面交互的编程语言

---

只包含浮点数的一组对象

---

代表HTML元素的一组JavaScript类

#### --answer--

让你与HTML文档交互的编程接口

### --question--

#### --text--

API代表什么？

#### --distractors--

附加编程接口

---

添加编程接口

---

另一个编程接口

#### --answer--

应用程序编程接口

### --question--

#### --text--

如何使用JavaScript找到所有缺少超链接的HTML锚点？

#### --distractors--

```js
document.findAll("link![href]");
```

---

```js
document.find("link:not([href])");
```

---

```js
document.querySelector("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

[textContent]属性做什么？

#### --distractors--

从所有HTML元素中删除文本内容

---

从你的JavaScript文件中删除文本内容

---

向JavaScript文件中的所有数组和对象添加文本

#### --answer--

返回元素的纯文本内容

### --question--

#### --text--

[innerHTML]属性做什么？

#### --distractors--

它是[Element]的一个属性，用于获取HTML元素的所有文本内容

---

它是[Element]的一个属性，用于向HTML添加注释

---

它是[Element]的一个属性，用于触发HTML中的事件

#### --answer--

它是[Element]的一个属性，用于设置或更新HTML标记的部分内容

### --question--

#### --text--

以下哪项将添加有效的事件监听器？

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

以下哪项不是[document]对象的有效方法？

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

以下哪项将[class]属性设置为[my-class]？

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

[Event]对象是什么？

#### --distractors--

它是用于从JavaScript项目中删除事件的特殊对象

---

它用于自动向JavaScript项目添加事件

---

它是处理数组时使用的特殊对象

#### --answer--

它是当用户以某种方式与网页交互时触发的有效载荷

### --question--

#### --text--

以下哪项是用于删除先前使用[addEventListener()]方法添加到元素的事件监听器的方法？

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

什么是内联事件处理器？

#### --distractors--

HTML元素上的特殊属性，用于取消所有事件

---

HTML元素上的特殊属性，仅用于触发JavaScript代码中的表单提交

---

HTML元素上的特殊属性，用于删除事件

#### --answer--

HTML元素上的特殊属性，用于在事件发生时执行JavaScript代码

### --question--

#### --text--

Web API的两个主要类别是什么？

#### --distractors--

Java API和第三方API

---

浏览器API和Rust API

---

系统API和第三方API

#### --answer--

浏览器API和第三方API

### --question--

#### --text--

当浏览器解析完HTML页面的所有元素时触发什么事件？

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

以下哪项是表示元素内联样式的属性？

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

以下哪项是使用内联事件处理器的正确方法？

#### --distractors--

```html
<button eventclick="validate()">Validate</button>
```

---

```html
<button click="validate()">Validate</button>
```

---

```html
<button clickevent="validate()">Validate</button>
```

#### --answer--

```html
<button onclick="validate()">Validate</button>
```

### --question--

#### --text--

以下哪种方法用于延迟指定时间的操作？

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

以下哪项可用于直接在JavaScript内部创建和控制动画？

#### --distractors--

允许动画API

---

控制动画API

---

创建动画API

#### --answer--

Web动画API

### --question--

#### --text--

Canvas API做什么？

#### --distractors--

用于在CSS文件中制作图形动画

---

仅用于从JavaScript文件中删除图形

---

用于从HTML文件中删除图形

#### --answer--

用于通过JavaScript文件操作图形

### --question--

#### --text--

什么是事件委托？

#### --distractors--

在事件被取消时监听事件的过程

---

在点击事件发生时监听事件的过程

---

在更改事件发生时监听事件的过程

#### --answer--

监听冒泡到父级的事件的过程

### --question--

#### --text--

在处理对话框和模态框时，以下哪项不是你可以使用的方法？

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`