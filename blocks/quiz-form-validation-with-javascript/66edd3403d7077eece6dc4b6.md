---
id: 66edd3403d7077eece6dc4b6
title: JavaScript表单验证测验
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪个方法用于检查[input]元素的值是否满足验证规则？

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

如果[checkValidity()]方法发现无效的[input]会发生什么？

#### --distractors--

浏览器自动提交表单。

---

什么都不会发生。

---

[input]值被清除。

#### --answer--

浏览器显示验证消息。

### --question--

#### --text--

哪个方法用于自定义验证失败时显示的消息？

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

提交表单时，必填字段的默认验证行为是什么？

#### --distractors--

即使必填字段为空，表单也会被提交。

---

表单提交但会给出警告。

---

表单重置[input]值。

#### --answer--

如果必填字段为空，浏览器会阻止表单提交。

### --question--

#### --text--

哪种操作不会导致HTML表单被检查错误？

#### --distractors--

更改[input]值。

---

提交表单。

---

调用[reportValidity()]。

#### --answer--

点击重置按钮。

### --question--

#### --text--

当所有表单输入都有效时，[checkValidity()]返回什么？

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

[checkValidity()]方法何时会阻止表单提交？

#### --distractors--

当表单没有提交按钮时。

---

当表单[action]为空时。

---

当表单方法为POST时。

#### --answer--

当表单至少有一个无效[input]时（并且阻止了表单提交的默认行为）。

### --question--

#### --text--

如果调用了[checkValidity()]，但某个字段验证失败会发生什么？

#### --distractors--

表单无错误地提交。

---

[input]值被清除。

---

表单立即关闭。

#### --answer--

无效字段会被高亮显示并出现消息。

### --question--

#### --text--

哪个操作会使[checkValidity()]运行？

#### --distractors--

重置表单。

---

更改[input]值。

---

点击锚标签。

#### --answer--

手动提交表单。

### --question--

#### --text--

在提交表单之前调用[checkValidity()]的目的是什么？

#### --distractors--

自动提交表单。

---

重新加载页面。

---

清除所有[input]字段。

#### --answer--

检查所有[input]元素是否符合其指定的约束条件。

### --question--

#### --text--

[e.preventDefault()]的主要目的是什么？

#### --distractors--

重新加载表单。

---

清除表单数据。

---

重置表单值。

#### --answer--

阻止事件的默认操作。

### --question--

#### --text--

在表单的提交事件上使用时，[e.preventDefault()]做什么？

#### --distractors--

它提交表单。

---

它清除所有表单字段。

---

它临时禁用表单。

#### --answer--

它阻止表单被提交。

### --question--

#### --text--

在哪种情况下[e.preventDefault()]最有用？

#### --distractors--

处理提交表单的按钮点击时。

---

关闭模态对话框时。

---

打开新页面时。

#### --answer--

提交表单并防止页面重新加载时。

### --question--

#### --text--

如果你忘记在表单提交事件上调用[e.preventDefault()]会发生什么？

#### --distractors--

所有输入字段都被设置为默认值。

---

表单不会提交。

---

表单数据丢失。

#### --answer--

表单提交并刷新页面。

### --question--

#### --text--

使用[e.preventDefault()]可以阻止哪种默认行为？

#### --distractors--

编辑表单。

---

渲染表单。

---

重新加载表单。

#### --answer--

由提交表单引起的默认行为。

### --question--

#### --text--

什么触发了表单的提交事件？

#### --distractors--

当输入字段改变时。

---

当表单被重置时。

---

当表单的操作被清除时。

#### --answer--

当点击表单的提交按钮或按下Enter键时。

### --question--

#### --text--

在JavaScript中通常在哪里处理提交事件？

#### --distractors--

在文本输入元素上。

---

在表单中的任何字段上。

---

在表单外的按钮元素上。

#### --answer--

在表单元素本身上。

### --question--

#### --text--

提交事件默认做什么？

#### --distractors--

验证表单但不提交。

---

清除表单字段。

---

取消表单提交。

#### --answer--

将表单数据发送到表单动作中指定的服务器。

### --question--

#### --text--

在JavaScript中，当表单提交时应该监听哪个事件来处理表单数据？

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

HTML表单中[method]属性的默认值是什么？

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`