---
id: 66edd4f31ff19bf5573bf64b
title: JavaScript函数式编程测验
challengeType: 8
dashedName: quiz-javascript-functional-programming
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是纯函数？

#### --distractors--

修改全局变量并返回[undefined]或[null]的函数

---

向控制台记录输出的函数

---

根据外部因素改变其行为并返回[null]的函数

#### --answer--

对于相同的输入总是返回相同输出且不产生副作用的函数

### --question--

#### --text--

函数式编程中的副作用是什么？

#### --distractors--

代码中的意外[TypeError]

---

执行时间过长并使程序崩溃的函数

---

产生无限循环并使程序崩溃的递归函数调用

#### --answer--

对函数外部可观察到的程序状态的改变

### --question--

#### --text--

函数式编程中的柯里化是什么？

#### --distractors--

设计测试以帮助确保代码100%无错误的技术

---

编写递归函数的更优化方式，使其运行速度比其他函数快两倍

---

构建、设计和测试代码以确保其符合ECMAScript制定标准的过程

#### --answer--

将具有多个参数的函数转换为一系列单参数函数的过程

### --question--

#### --text--

以下纯函数的输出是什么？

```js
const add = (a, b) => a + b;
console.log(add(2, 5));
```

#### --distractors--

`3`

---

`4`

---

`undefined`

#### --answer--

`7`

### --question--

#### --text--

以下哪项是柯里化的例子？

#### --distractors--

```js
const curriedAverage = (a, b, c) => a + b + c / 3
```

---

```js
const curriedAverage = (a, b, c) => a + b + c
```

---

```js
function curriedAverage(a) {
  return a 
}

function curried(c) {
  return c
}
```

#### --answer--

```js
function curriedAverage(a) {
  return function(b) {
    return function(c) {
      return (a + b + c) / 3;
    };
  };
}
```

### --question--

#### --text--

以下哪项是函数式编程的关键原则？

#### --distractors--

频繁修改全局变量

---

广泛使用可变数据结构

---

强调面向对象的继承

#### --answer--

避免副作用并使用不可变数据

### --question--

#### --text--

什么是不纯函数？

#### --distractors--

返回对象的函数

---

没有副作用的函数

---

返回[null]的函数

#### --answer--

具有副作用的函数

### --question--

#### --text--

以下哪项是调用柯里化函数的正确方式？

#### --distractors--

```js
curriedAverage(2 3 4);
```

---

```js
curriedAverage(2)==(3)==(4);
```

---

```js
curriedAverage(2)=>(3)=>(4);
```

#### --answer--

```js
curriedAverage(2)(3)(4);
```

### --question--

#### --text--

以下哪项是不纯函数的例子？

#### --distractors--

```js
function example(num) {
  return num;
}
```

---

```js
function sum(num1, num2) {
 return num1 + num2
}
```

---

```js
function addToTotal(value) {
  let total = 0;
  total += value;
  return total;
}
```

#### --answer--

```js
let total = 0;
function addToTotal(value) {
  total += value;
  return total;
}
```

### --question--

#### --text--

以下哪项不是副作用的例子？

#### --distractors--

写入文件

---

修改全局变量

---

进行API调用

#### --answer--

返回两个值的和