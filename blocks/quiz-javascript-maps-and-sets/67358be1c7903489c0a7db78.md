---
id: 67358be1c7903489c0a7db78
title: JavaScript映射和集合测验
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript中的Set是什么？

#### --distractors--

允许重复值的数据结构。

---

只存储字符串和数字的数据结构。

---

不允许删除其元素的集合。

#### --answer--

值只能出现一次的集合。

### --question--

#### --text--

以下哪个方法在Map中可用但在Set中不可用？

#### --distractors--

`has()`。

---

`clear()`。

---

`values()`。

#### --answer--

`set()`。

### --question--

#### --text--

使用哪个方法向Set添加元素？

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

如何检查Set是否包含某个值？

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

以下哪项是Set的绝佳用例？

#### --distractors--

管理数据库中的键值对。

---

创建有序项目列表。

---

在浏览器本地存储中存储分层数据结构。

#### --answer--

从数组中消除重复项。

### --question--

#### --text--

JavaScript中的WeakSet是什么？

#### --distractors--

只允许原始数据类型的类似Set的集合。

---

允许重复值的类似Set的集合。

---

阻止移除值的类似Set的集合。

#### --answer--

只存储对象的类似Set的集合。

### --question--

#### --text--

Set和WeakSet之间的一个区别是什么？

#### --distractors--

WeakSet可以存储任何类型的值，包括数字。

---

Set不允许重复值，但WeakSet允许。

---

Set允许垃圾回收，而WeakSet不允许。

#### --answer--

Set可以保存任何类型的值，而WeakSet只保存对象。

### --question--

#### --text--

JavaScript中的Map是什么？

#### --distractors--

只允许唯一值的集合。

---

只能使用字符串作为键的集合。

---

不允许删除其项目的集合。

#### --answer--

任何类型的键值对的集合。

### --question--

#### --text--

使用哪个方法向Map插入键值对？

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

以下代码将输出什么？

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

Map与JavaScript中的Object有什么区别？

#### --distractors--

Map只允许数字作为键。

---

对象允许任何类型作为键，而Map只允许字符串。

---

Map不支持迭代，而对象支持。

#### --answer--

Map允许任何类型的键，但对象只允许字符串和符号。

### --question--

#### --text--

如何从Map中移除键值对？

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Map的`size`属性返回什么？

#### --distractors--

Map中的索引数。

---

Map中的唯一值数。

---

Map使用的总内存。

#### --answer--

Map中的键值对数。

### --question--

#### --text--

JavaScript中的WeakMap是什么？

#### --distractors--

允许原始值作为键的类似Map的集合。

---

阻止删除项目的类似Map的集合。

---

自动排序其键的类似Map的集合。

#### --answer--

对键具有弱引用的类似Map的集合。

### --question--

#### --text--

Map和WeakMap之间的一个区别是什么？

#### --distractors--

Map只允许原始类型作为键，而WeakMap只允许对象。

---

Map允许垃圾回收，而WeakMap不允许。

---

WeakMap允许重复键，而Map不允许。

#### --answer--

Map是可迭代的，而WeakMap不是。

### --question--

#### --text--

以下哪个方法在WeakMap上不存在？

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

以下代码的输出是什么？

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

如何遍历Set中的值？

#### --distractors--

通过使用`.map()`方法。

---

通过使用`.reduce()`方法。

---

通过使用`.filter()`方法。

#### --answer--

通过使用`.forEach()`方法。

### --question--

#### --text--

Set的哪两个方法返回包含某个Set值的`SetIterator`？

#### --distractors--

`entries()`和`values()`。

---

`forEach()`和`values()`。

---

`has()`和`add()`。

#### --answer--

`keys()`和`values()`。

### --question--

#### --text--

以下代码的输出是什么？

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`