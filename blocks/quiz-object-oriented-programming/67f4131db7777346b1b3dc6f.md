---
id: 67f4131db7777346b1b3dc6f
title: 面向对象编程测验
challengeType: 8
dashedName: quiz-object-oriented-programming
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么作为创建对象的蓝图？

#### --distractors--

函数

---

变量

---

循环

#### --answer--

类

### --question--

#### --text--

Python 使用哪个模块来实现抽象类？

#### --distractors--

`Time`

---

`xyz`

---

`Random`

#### --answer--

`abc`

### --question--

#### --text--

什么定义了对象的数据和行为？

#### --distractors--

函数和类。

---

变量和循环。

---

种子器和数据库。

#### --answer--

属性和方法。

### --question--

#### --text--

什么是单一继承？

#### --distractors--

子类从单个函数继承属性和方法。

---

子类从多个函数继承属性和方法。

---

子类从多个类继承属性和方法。

#### --answer--

子类从单个类继承属性和方法。

### --question--

#### --text--

以下哪项不是面向对象编程的关键原则之一？

#### --distractors--

封装

---

继承

---

抽象

#### --answer--

不要重复自己 (DRY)

### --question--

#### --text--

使用双下划线前缀和单下划线前缀属性和方法的区别是什么？

#### --distractors--

单下划线完全隐藏属性，双下划线使其仅在方法内部可读。

---

单下划线意味着私有，双下划线意味着受保护。

---

单下划线使它们成为公共的，双下划线使它们成为静态的。

#### --answer--

单下划线只是内部使用的约定，双下划线触发名称修饰以防止意外的属性和方法覆盖。

### --question--

#### --text--

哪个面向对象编程概念让你隐藏复杂的实现细节，只显示对象或系统的基本特征？

#### --distractors--

继承

---

多态

---

封装

#### --answer--

抽象

### --question--

#### --text--

使用哪个装饰器来创建 setter？

#### --distractors--

`@<property_name>.creator`

---

`@<method_name>.creater`

---

`@<method_name>.creator`

#### --answer--

`@<property_name>.setter`

### --question--

#### --text--

哪个面向对象编程概念让你将对象的内部状态隐藏在一组充当门的公共方法和属性后面？

#### --distractors--

多态

---

抽象

---

单一继承

#### --answer--

封装

### --question--

#### --text--

什么是 getter？

#### --distractors--

更新或更改属性值的方法。

---

从对象中删除属性的方法。

---

在对象中创建新属性的方法。

#### --answer--

检索或返回属性值的方法。

### --question--

#### --text--

什么是 setter？

#### --distractors--

检索属性值的方法。

---

删除属性值的方法。

---

从对象中删除属性的方法。

#### --answer--

设置或更新属性值的方法。

### --question--

#### --text--

什么让你可以删除使用 setter 和 getter 设置和获取的值？

#### --distractors--

移除器

---

橡皮擦

---

延迟器

#### --answer--

删除器

### --question--

#### --text--

什么促进代码重用，提供清晰的层次结构，并允许在不重写一切的情况下定制行为？

#### --distractors--

DRY

---

WET

---

抽象

#### --answer--

继承

### --question--

#### --text--

以下哪个是正确的继承语法？

#### --distractors--

```py
class Parent:
    pass
class Child(InheritParent):
    pass
```

---

```py
class Parent:
    pass
class Child(Inheriter):
    pass
```

---

```py
class Child:
   pass
class Parent(Parent):
   pass
```

#### --answer--

```py
class Parent:
   pass
class Child(Parent):
   pass
```

### --question--

#### --text--

什么连接 setters 和 getters？

#### --distractors--

方法

---

函数

---

类

#### --answer--

属性

### --question--

#### --text--

Python 通过添加下划线和类名作为前缀来重命名以双下划线为前缀的属性的过程叫什么？

#### --distractors--

名称混合

---

多态

---

抽象

#### --answer--

名称修饰

### --question--

#### --text--

哪个函数让你在子类中调用父类的方法？

#### --distractors--

`constructor()`

---

`__init__()`

---

`property()`

#### --answer--

`super()`

### --question--

#### --text--

以下哪个陈述最好地描述了面向对象编程中的多态？

#### --distractors--

它允许一个类拥有具有不同属性和方法的多个构造函数。

---

它允许一个类从另一个类继承属性和方法。

---

它允许使用 `class` 关键字创建对象。

#### --answer--

它允许不同的类使用相同的方法名，在调用时执行不同的操作。

### --question--

#### --text--

为什么 Python 会修饰以双下划线为前缀的属性名称？

#### --distractors--

使属性完全私有，无法从类外部访问。

---

通过更有效地存储属性来提高性能。

---

自动将属性转换为只读属性。

#### --answer--

防止在使用继承时子类中意外的属性和方法覆盖。

### --question--

#### --text--

为什么属性被用于 getters 和 setters 而不是方法？

#### --distractors--

使代码更长更明确。

---

完全阻止对值的访问。

---

自动记录每次值的变化。

#### --answer--

允许使用点符号直接像属性一样访问，以获得更好的可读性。