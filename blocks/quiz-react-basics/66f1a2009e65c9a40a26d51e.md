---
id: 66f1a2009e65c9a40a26d51e
title: React 基础知识测验
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项不是 JavaScript 库或框架？

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

以下哪项不是 React 中条件渲染的方法？

#### --distractors--

三元运算符。

---

逻辑 `&&` 运算符。

---

`if` 语句。

#### --answer--

`setTimeout`

### --question--

#### --text--

在 React 中渲染列表时添加 `key` 属性的主要目的是什么？

#### --distractors--

为每个列表项应用唯一的 CSS 样式。

---

作为元素的文本内容。

---

为列表中的每个特定项目添加事件监听器。

#### --answer--

帮助 React 识别项目以实现高效渲染。

### --question--

#### --text--

项目的包和库安装在哪里？

#### --distractors--

计算机的用户目录。

---

`package.json` 文件。

---

`src` 文件夹。

#### --answer--

`node_modules` 文件夹。

### --question--

#### --text--

关于 JSX，以下哪项是正确的？

#### --distractors--

JSX 必须始终写在与 JavaScript 分离的文件中。

---

JSX 不能使用 JavaScript 表达式。

---

JSX 不推荐用于 React 应用程序。

#### --answer--

JSX 允许在 JavaScript 代码中编写 HTML。

### --question--

#### --text--

以下哪项是 SPA 的常见问题？

#### --distractors--

由于持续页面渲染，服务器资源使用更多。

---

由于频繁的 DOM 更新，浏览器缓存变得不可靠。

---

多次页面加载导致状态管理不一致。

#### --answer--

屏幕阅读器难以处理动态更新的内容。

### --question--

#### --text--

单页应用程序通常如何更新视图？

#### --distractors--

通过完全重新加载服务器。

---

通过重新加载整个 HTML 文档。

---

仅使用服务器端渲染。

#### --answer--

通过仅更新页面的必要部分。

### --question--

#### --text--

以下哪个命令以开发模式启动 React 项目？

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

以下哪项不是有效的 JSX 元素？

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

哪个 JavaScript 数组方法常用于将数据数组转换为 JSX 元素数组？

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

在 React 中传递 `key` 属性的最佳实践是什么？

#### --distractors--

在所有情况下都使用数组索引。

---

在每次渲染时随机生成键。

---

完全省略 `key` 属性。

#### --answer--

使用来自数据的唯一且稳定的 ID。

### --question--

#### --text--

哪个命令安装 `package.json` 中列出的依赖项？

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

如何向子组件传递 `name` 属性？

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

在 React 中使用展开运算符传递多个属性的正确方式是什么？

#### --distractors--

`<Child person={name..., age..., city...} />`。

---

`<Child spreader={...person} />`。

---

`<Child spread={person} />`。

#### --answer--

`<Child {...person} />`。

### --question--

#### --text--

React 的构建块是什么？

#### --distractors--

函数和模块。

---

DOM 元素和事件。

---

类和对象。

#### --answer--

可重用组件。

### --question--

#### --text--

在 React 中添加内联样式的正确方式是什么？

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

条件渲染 React 组件的正确方式是什么？

#### --distractors--

在 JSX 内部使用 `if` 语句。

---

在 `render()` 方法内声明新组件。

---

在 JSX 内部使用 `while` 循环。

#### --answer--

在 JSX 内使用三元运算符或逻辑 `&&` 运算符。

### --question--

#### --text--

使用哪个关键字将组件引入另一个文件？

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

SPAs 的全称是什么？

#### --distractors--

服务器页面架构。

---

选择性页面应用程序。

---

静态渐进式应用。

#### --answer--

单页应用程序。

### --question--

#### --text--

哪种 JavaScript 语法扩展用于描述 React 中的 UI？

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX