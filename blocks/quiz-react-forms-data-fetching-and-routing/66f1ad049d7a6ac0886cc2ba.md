---
id: 66f1ad049d7a6ac0886cc2ba
title: React 表单、数据获取和路由测验
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项是受控输入的示例？

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

以下哪项是非受控输入的示例？

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

以下哪项不是 React 中常用的获取数据的方式？

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

以下哪个钩子通常与在 React 中获取数据相关联？

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

什么是记忆化？

#### --distractors--

一种用于查找图中两个顶点之间最短路径的常用算法。

---

一种特殊的优化技术，用于优化所有受控和非受控输入的性能。

---

一种分治算法，用于递归地对子数组进行排序并将它们合并在一起。

#### --answer--

一种优化技术，其中基于特定参数缓存（记住）昂贵函数调用的结果。

### --question--

#### --text--

以下哪个钩子用于记忆计算值？

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

以下哪项使用了正确的钩子来记忆函数引用？

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
```

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
```

---

```js
useState(() => {
 // code goes here
}, [dependency]);
```

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
```

### --question--

#### --text--

什么是依赖？

#### --distractors--

依赖是在 React 应用程序中使用的常用测试库。

---

依赖是仅在服务器上运行的 React 组件类型。

---

依赖是 React 应用程序中用于将 React 代码编译成 JavaScript 的特殊编译器。

#### --answer--

依赖是指应用程序中的一个组件或模块依赖于另一个组件或模块才能正常工作的关系。

### --question--

#### --text--

什么是包管理器？

#### --distractors--

用于更新应用程序中安装的第三方包的工具。

---

用于管理项目内创建的包的工具。

---

用于将依赖从 `node_modules` 文件夹移动到特殊配置文件夹的工具。

#### --answer--

用于安装、更新和删除依赖的工具。

### --question--

#### --text--

以下哪项不是 JavaScript 生态系统中常用的包管理器？

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

以下哪项是包含项目元数据的关键配置文件？

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

以下哪项是常用的 React 框架？

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

以下哪项是使用 React Router 向 React 应用程序添加路由的正确方式？

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

以下哪项是创建 React Router 中动态路由的正确方式？

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

以下哪个钩子用于访问 URL 路径中的动态参数？

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

以下哪项是在 Next.js 中创建自定义请求处理器的正确方式？

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

什么是属性钻取？

#### --distractors--

创建指定状态应该如何响应特定属性变化的 reducer 的过程。

---

将属性从子组件传递给父组件的过程。

---

为 React 应用程序创建上下文提供者并使用它将属性从父组件发送到子组件的过程。

#### --answer--

将属性从父组件传递给深度嵌套的子组件的过程，即使某些子组件不需要这些属性。

### --question--

#### --text--

以下哪项不是 React 应用程序常用的状体管理解决方案？

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

React 开发者工具是用来做什么的？

#### --distractors--

这些工具仅用于检查 React 应用程序中的可访问性问题。

---

这些工具用于为 React 组件添加额外的测试。

---

这些工具用于记忆应用程序中的所有 React 组件。

#### --answer--

这些工具用于检查 React 组件并识别性能问题。

### --question--

#### --text--

什么是 React 服务器组件？

#### --distractors--

这些是在服务器上专门管理上下文的组件。

---

这些是在服务器上专门渲染的记忆化组件。

---

这些是在服务器上专门管理状态的组件。

#### --answer--

这些是在服务器上专门渲染的组件。