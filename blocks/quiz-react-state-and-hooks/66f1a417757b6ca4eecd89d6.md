---
id: 66f1a417757b6ca4eecd89d6
title: React 状态和钩子测验
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项是在 React 中向按钮添加点击事件的正确方式？

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

什么是 React 合成事件？

#### --distractors--

一种仅用于 `inputs` 和 `submit` 事件的 JSX 元素类型。

---

一种仅用于 `submit` 事件的 JSX 元素类型。

---

一种改变事件传播冒泡阶段以使其更用户友好的方式。

#### --answer--

围绕 `click` 和 `submit` 等原生事件的包装器。

### --question--

#### --text--

以下哪项是用于命名事件处理函数的常见前缀？

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

React 渲染的三个主要阶段是什么？

#### --distractors--

握手、加载和水合。

---

获取、解码和执行。

---

挂载、更新和卸载。

#### --answer--

触发、渲染和提交。

### --question--

#### --text--

以下哪项是使用 `useState` 钩子的正确方式？

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

以下哪项不是用于处理 React 事件的有效属性？

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

以下哪项是更新数组状态的正确方式？

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front End");
```

---

```js
setCertificates.push("Front End");
```

---

```js
setCertificates().append("Front End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front End"]);
```

### --question--

#### --text--

这个函数有什么问题？

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

在嵌套函数中不允许更新 `useState` 变量。

---

`previousState` 需要用括号包围。

---

在状态设置函数中不应使用箭头函数。

#### --answer--

`previousState` 必须在 `name` 之前展开，以确保其他属性不会被覆盖。

### --question--

#### --text--

以下哪项是更新状态以从数组中删除项目的正确方式？

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

React 中的 `ref` 属性通常用于什么？

#### --distractors--

用于审计 React 组件中的性能问题。

---

用于审计 React 组件中的可访问性问题。

---

用于删除 DOM 节点。

#### --answer--

用于访问 DOM 节点。

### --question--

#### --text--

以下哪项是访问 `ref` 当前值的正确方式？

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

这条消息将被记录到控制台多少次？

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

三次。

---

两次。

---

无，因为依赖数组为空。

#### --answer--

仅在初始渲染时记录一次。

### --question--

#### --text--

React 自定义钩子通常用于什么？

#### --distractors--

用于执行应用程序的最复杂功能。

---

通常不使用，更喜欢内置钩子。

---

用于替换以前版本 React 中的生命周期方法。

#### --answer--

有助于在组件间重用有状态逻辑。

### --question--

#### --text--

这条消息将被记录到控制台多少次？

```js
useEffect(() => {
  console.log("I love MasterPuti!");
});
```

#### --distractors--

在提交表单或发生点击事件时都会记录。

---

将记录两次。

---

只会记录一次。

#### --answer--

在组件每次渲染时都会记录。

### --question--

#### --text--

以下哪项是防止 `onSubmit` 事件浏览器刷新的正确方式？

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--

```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

以下哪个钩子通常用于获取数据？

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

以下哪项不是原生 Event 对象上的有效属性？

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

当 React 检测到某些内容已更改且用户界面 (UI) 可能需要更新时，会发生以下哪个渲染阶段？

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

以下哪个阶段指的是 React 从虚拟 DOM 获取准备好的更改并将其应用到真实 DOM 的时候？

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

`useEffect` 中的 `dependencies` 的作用是什么？

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

可选的 `dependencies` 参数不影响 `useEffect` 的任何内容。

---

可选的 `dependencies` 参数使效果在无限循环中运行。

---

可选的 `dependencies` 参数用于防止效果运行。

#### --answer--

可选的 `dependencies` 参数控制效果何时运行。