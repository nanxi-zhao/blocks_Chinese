---
id: 680a5e7f3d47715196b12ca0
title: 步骤 5
challengeType: 20
dashedName: step-5
---

# --description--

现在你已经有了完整的移动字母表，移除 `print(shifted_alphabet)` 调用。

# --hints--

你应该从代码中移除 `print(shifted_alphabet)`。

```js
({ test: () => assert(runPython(`not _Node(_code).has_call("print(shifted_alphabet)")`)) })
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
alphabet = 'abcdefghijklmnopqrstuvwxyz'
shift = 5
shifted_alphabet = alphabet[shift:] + alphabet[:shift]
print(shifted_alphabet)
--fcc-editable-region--
```
