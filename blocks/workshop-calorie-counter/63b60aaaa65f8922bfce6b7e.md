---
id: 63b60aaaa65f8922bfce6b7e
title: 步骤 9
challengeType: 0
dashedName: step-9
---

# --description--

在你的 `span` 元素内，创建一个用于 `entry-dropdown` 的 `label`，并将文本设置为 `Add food or exercise:`。然后创建一个 `id` 为 `entry-dropdown`、`name` 为 `options` 的 `select` 元素。在其下方添加一个 `id` 为 `add-entry`、文本为 `Add Entry` 的 `button` 元素。

请将该 `button` 的 `type` 属性设置为 `button`，以避免表单自动提交。

# --hints--

在 `.controls > span` 中应包含一个 `label` 元素。

```js
assert.exists(document.querySelector('.controls > span > label'));
```

该 `label` 的 `for` 属性应为 `entry-dropdown`。

```js
assert.equal(document.querySelector('.controls > span > label')?.getAttribute('for'), 'entry-dropdown');
```

该 `label` 的文本应为 `Add food or exercise:`。

```js
assert.equal(document.querySelector('.controls > span > label')?.innerText, 'Add food or exercise:');
```

在相同位置应添加一个 `select` 元素。

```js
assert.exists(document.querySelector('.controls > span > select'));
```

该 `select` 应位于 `label` 之后。

```js
assert.equal(document.querySelector('.controls > span > select')?.previousElementSibling?.tagName, 'LABEL');
```

该 `select` 的 `id` 应为 `entry-dropdown`，`name` 应为 `options`。

```js
assert.equal(document.querySelector('.controls > span > select')?.getAttribute('id'), 'entry-dropdown');
```

```js
assert.equal(document.querySelector('.controls > span > select')?.getAttribute('name'), 'options');
```

该 `select` 应为空，并包含闭合 `</select>` 标签。

```js
assert.equal(document.querySelector('.controls > span > select')?.innerHTML?.trim(), '');
```

接着应添加一个 `button` 元素。

```js
assert.exists(document.querySelector('.controls > span > button'));
```

该 `button` 应位于 `select` 之后，且 `id` 应为 `add-entry`。

```js
assert.equal(document.querySelector('.controls > span > button')?.previousElementSibling?.tagName, 'SELECT');
```

```js
assert.equal(document.querySelector('.controls > span > button')?.getAttribute('id'), 'add-entry');
```

该 `button` 的文本应为 `Add Entry`。

```js
assert.equal(document.querySelector('.controls > span > button')?.innerText, 'Add Entry');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>Calorie Counter</title>
  </head>
  <body>
    <main>
      <h1>Calorie Counter</h1>
      <div class="container">
        <form id="calorie-counter">
          <label for="budget">Budget</label>
          <input
            type="number"
            min="0"
            id="budget"
            placeholder="Daily calorie budget"
            required
          />
          <fieldset id="breakfast">
            <legend>Breakfast</legend>
            <div class="input-container"></div>
          </fieldset>
          <fieldset id="lunch">
            <legend>Lunch</legend>
            <div class="input-container"></div>
          </fieldset>
          <fieldset id="dinner">
            <legend>Dinner</legend>
            <div class="input-container"></div>
          </fieldset>
          <fieldset id="snacks">
            <legend>Snacks</legend>
            <div class="input-container"></div>
          </fieldset>
          <fieldset id="exercise">
            <legend>Exercise</legend>
            <div class="input-container"></div>
          </fieldset>
--fcc-editable-region--
          <div class="controls">
            <span>

            </span>
          </div>
--fcc-editable-region--
        </form>
      </div>
    </main>
  </body>
</html>
```

```css
:root {
  --light-grey: #f5f6f7;
  --dark-blue: #0a0a23;
  --fcc-blue: #1b1b32;
  --light-yellow: #fecc4c;
  --dark-yellow: #feac32;
  --light-pink: #ffadad;
  --dark-red: #850000;
  --light-green: #acd157;
}

body {
  font-family: "Lato", Helvetica, Arial, sans-serif;
  font-size: 18px;
  background-color: var(--fcc-blue);
  color: var(--light-grey);
}

h1 {
  text-align: center;
}

.container {
  width: 90%;
  max-width: 680px;
}

h1,
.container,
.output {
  margin: 20px auto;
}

label,
legend {
  font-weight: bold;
}

.input-container {
  display: flex;
  flex-direction: column;
}

button {
  cursor: pointer;
  text-decoration: none;
  background-color: var(--light-yellow);
  border: 2px solid var(--dark-yellow);
}

button,
input,
select {
  min-height: 24px;
  color: var(--dark-blue);
}

fieldset,
label,
button,
input,
select {
  margin-bottom: 10px;
}

.output {
  border: 2px solid var(--light-grey);
  padding: 10px;
  text-align: center;
}

.hide {
  display: none;
}

.output span {
  font-weight: bold;
  font-size: 1.2em;
}

.surplus {
  color: var(--light-pink);
}

.deficit {
  color: var(--light-green);
}
```

```js

```
