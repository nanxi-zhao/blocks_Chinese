---
id: 655cd899f8de09431eabb40c
title: 步骤 1
challengeType: 20
dashedName: step-1
---

# --description--
归并排序（Merge Sort）是一种超优雅的“分而治之”排序算法：先把大列表不停地拆成更小的子列表，然后各自独立排序，最后再把已经排序好的小列表像拼图一样合并回去，得到整齐的结果。是不是很有条理的感觉～

第一步，创建一个名为 `merge_sort` 的函数，参数是 `array`。它将负责对数字列表进行排序。现在先在函数体里写上 `pass` 做占位，我们接下来会一步步填充它，让它逐渐强大起来！加油，你已迈出漂亮的第一步！💪

# --hints--
需要声明一个只有 `array` 参数的函数 `merge_sort`，并在函数体里暂时使用 `pass` 占位。

```js
({
    test: () => assert(runPython(`_Node(_code).find_function("merge_sort").has_args("array")`))
})
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--

--fcc-editable-region--
```
