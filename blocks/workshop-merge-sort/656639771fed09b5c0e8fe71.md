---
id: 656639771fed09b5c0e8fe71
title: 步骤 5
challengeType: 20
dashedName: step-5
---

# --description--
你已经成功把原列表一分为二，接下来要让它们继续被拆分，直到每个小列表只剩一个元素——因为单元素列表天然是“有序”的！

为此，请在函数内部“递归调用”自己：调用 `merge_sort(left_part)`。这是让算法继续深入分解的第一步。勇敢迈出这一小步，你的排序函数马上要显现威力啦～💡

# --hints--
需要在函数体靠下的位置调用 `merge_sort(left_part)`。

```js
({
    test: () => assert(runPython(`_Node(_code).find_function("merge_sort").is_ordered("right_part = array[middle_point:]", "merge_sort(left_part)")`
))
})
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
def merge_sort(array):
    middle_point = len(array) // 2
    left_part = array[:middle_point]
    right_part = array[middle_point:]

--fcc-editable-region--
```
