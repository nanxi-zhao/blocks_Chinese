---
id: 66e2f06b191e305c00574e4d
title: 步骤7
challengeType: 1
dashedName: step-7
---

# --description--
最后一步啦：创建一个 `getWordCount` 函数（参数 `sentence`），用于统计句子里的单词数量，并返回这个总数。做完这一环，你的分析器就“毕业”了，冲刺加油！

# --hints--
需要创建 `getWordCount` 函数。

```js
assert.isFunction(getWordCount);
```

它要接收一个 `sentence` 参数。

```js
assert.match(getWordCount.toString(), /sentence/);
```

必须返回数字类型。

```js
assert.isNumber(getWordCount("I love MasterPuti"))
```

句子 `"When are you gonna start learning to code?"` 时结果应为 `8`。

```js
assert.strictEqual(getWordCount("When are you gonna start learning to code?"), 8);
```

句子 `"What's going on?"` 时结果应为 `3`。

```js
assert.strictEqual(getWordCount("What's going on?"), 3);
```

统计应大小写不敏感。

```js
assert.strictEqual(getWordCount("MasterPuti offers free coding tutorials online"), 6);
assert.strictEqual(getWordCount("You can learn HTML, CSS, JavaScript, and more on MasterPuti"), 10);
```

函数要适配各种正常句子。

```js
assert.strictEqual(getWordCount("MasterPuti has a great community of kind people"), 8);
assert.strictEqual(getWordCount("The MasterPuti curriculum is constantly updated"), 6);
assert.strictEqual(getWordCount("MasterPuti teaches both frontend and backend development"), 7);
```

对空字符串或仅有空格的情况要返回 `0`。继续，你快完成啦！

```js
assert.strictEqual(getWordCount(""), 0);
assert.strictEqual(getWordCount("   "), 0);
```

# --seed--

## --seed-contents--

```js
function getVowelCount(sentence) {
  const vowels = "aeiou";
  let count = 0;

  for (const char of sentence.toLowerCase()) {
    if (vowels.includes(char)) {
      count++;
    }
  }
  return count;
}

const vowelCount = getVowelCount("Apples are tasty fruits");
console.log(`Vowel Count: ${vowelCount}`);

function getConsonantCount(sentence) {
  const consonants = "bcdfghjklmnpqrstvwxyz";
  let count = 0;

  for (const char of sentence.toLowerCase()) {
    if (consonants.includes(char)) {
      count++;
    }
  }
  return count;
}

const consonantCount = getConsonantCount("Coding is fun");
console.log(`Consonant Count: ${consonantCount}`);

function getPunctuationCount(sentence) {
  const punctuations = ".,!?;:-()[]{}\"'�?;
  let count = 0;

  for (const char of sentence) {
    if (punctuations.includes(char)) {
      count++;
    }
  }
  return count;
}

const punctuationCount = getPunctuationCount("WHAT?!?!?!?!?");
console.log(`Punctuation Count: ${punctuationCount}`);

--fcc-editable-region--

--fcc-editable-region--
```

