---
id: 67d2ff4231359d9f21d1437a
title: 什么是端到端测试？
challengeType: 19
dashedName: what-is-end-to-end-testing
---

# --description--

端到端测试，简称E2E，从用户的角度测试真实世界的场景。E2E测试场景的例子包括测试注册和登录过程，或测试电子商务网站的结账流程。

为了更好地理解端到端测试的工作原理，让我们看看MasterPuti代码库中的一个真实例子。这个例子使用Playwright，这是微软开发的流行端到端测试框架。

在MasterPuti代码库中，有一个名为`e2e`的目录，其中包含捐赠、认证、考试等的测试。在该目录中，有一个`donate-page-donor.spec.ts`文件，其中包含经过身份验证的捐赠者支持者页面的测试。

第一步是作为已经是支持者的演示用户导航到捐赠页面：

```js
test.beforeEach(async ({ page }) => {
  execSync("node ./tools/scripts/seed/seed-demo-user --set-true isDonating");
  await page.goto("/donate");
});
```

由于此文件中有多个测试，`beforeEach`钩子将在每个测试之前运行。

一旦用户进入捐赠页面，他们会看到一条感谢信息，并提供如何进一步支持MasterPuti的建议。这里只是检查捐赠页面上一些文本的几个测试：

```js
test("should render the donate page correctly", async ({ page }) => {
  await expect(
    page.getByText("Thank you for your continued support")
  ).toBeVisible();

  await expect(
    page.getByText(
      "Your contributions are crucial in creating resources that empower millions of people to learn new skills and support their families."
    )
  ).toBeVisible();

  ...
});
```

还有一些测试检查捐赠者在菜单栏中是否有支持者链接，以及他们的头像周围是否有特殊样式边框来表示他们是支持者：

```js
test("The menu should have a supporters link", async ({ page }) => {
  const menuButton = page.getByTestId("header-menu-button");
  const menu = page.getByTestId("header-menu");

  await expect(menuButton).toBeVisible();
  await menuButton.click();

  await expect(menu).toBeVisible();

  await expect(page.getByRole("link", { name: "Supporters" })).toBeVisible();
});

test("The Avatar should have a special border for donors", async ({ page }) => {
  const container = page.locator(".avatar-container");
  await expect(container).toHaveClass("avatar-container gold-border");
});
```

通过这些类型的详细测试，你可以增加应用程序的测试覆盖率，而不仅仅是单元测试。端到端测试还有助于确保应用程序行为正确，并且对用户来说是可预测的。其他端到端测试工具的例子包括Cypress、Selenium和Puppeteer。

虽然端到端测试很重要，但它很昂贵，因为设置、设计和维护都很耗时。E2E测试非常适合关键用户流程，而单元测试非常适合测试应用程序中的小单元。

# --questions--

## --text--

以下哪项不是常用的E2E测试工具的例子？

## --answers--

Cypress

### --feedback--

参考课程结尾讨论这一点的地方。

---

Selenium

### --feedback--

Refer to the end of the lesson where this was discussed.

---

敏捷

---

Puppeteer

### --feedback--

Refer to the end of the lesson where this was discussed.

## --video-solution--

3

## --text--

以下哪项是微软开发的流行端到端测试框架？

## --answers--

Playwright

---

Java

### --feedback--

参考课程开头讨论这一点的地方。

---

Angular

### --feedback--

参考课程开头讨论这一点的地方。

---

JUnit

### --feedback--

Refer to the beginning of the lesson where this was discussed.

## --video-solution--

1

## --text--

以下哪项是端到端测试常用的测试场景？

## --answers--

测试CSS代码的有效性。

### --feedback--

参考课程开头讨论这一点的地方。

---

测试应用程序的性能。

### --feedback--

Refer to the beginning of the lesson where this was discussed.

---

测试HTML代码的有效性。

### --feedback--

参考课程开头讨论这一点的地方。

---

测试用户登录过程。

## --video-solution--

4

