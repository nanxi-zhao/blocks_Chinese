---
id: 672c37498952920879c43de9
title: 如何使用属性选择器定位带有 lang 和 data-lang 属性的元素？
challengeType: 19
dashedName: how-to-use-the-attribute-selector-to-target-elements-with-the-lang-and-data-lang-attributes
---

# --interactive--

在构建多语言网站或处理自定义数据属性时，您通常需要根据元素包含的语言或特定数据值来设置元素的样式。

[lang] 和 [data-lang] 属性通常用于这些目的，CSS 中的属性选择器允许您有效地基于这些属性应用样式。

[lang] 属性在 HTML 中用于指定元素内内容的语言。您可能希望根据编写语言的不同来区分元素的样式，特别是在多语言网站上。

以下是使用属性选择器定位具有特定 [lang] 属性的元素的示例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p lang="en">这是一个英文段落。</p>
<p lang="fr">Ceci est un paragraphe en français.</p>
```

```css
p[lang="en"] {
  font-style: italic;
}
```

:::

此 CSS 规则对 [lang] 属性设置为英语（[en]）的任何段落元素应用斜体样式。这对于强调多语言文档中的英语文本很有用。

同样，您可以使用属性选择器定位具有 [data-lang] 属性的元素。

像 [data-lang] 这样的自定义数据属性通常用于在元素中存储附加信息，例如指定文本特定部分中使用的语言。

以下是使用 CSS 基于 [data-lang] 属性定位和设置元素样式的方法：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p data-lang="fr">Ceci est un paragraphe en français.</p>
<p data-lang="en">This is a paragraph in English.</p>
```

```css
p[data-lang="fr"] {
  color: blue;
}
```

:::

在这种情况下，任何 [data-lang] 属性设置为法语（[fr]）的 [p] 元素都将具有蓝色文本。这允许您根据存储在 [data-lang] 属性中的语言信息快速识别和设置内容部分的样式。

像这样的属性选择器提供了一种强大的方式，可以根据嵌入 HTML 中的元数据应用条件样式，使您的网页更具动态性和上下文感知能力。

# --questions--

## --text--

您会使用哪个 CSS 选择器来定位所有 [lang] 属性设置为英语的段落？

## --answers--

[p[lang="en"]]

---

[p[lang]]

### --feedback--

考虑一下针对具有特定 [lang] 属性值的元素的选择器。

---

[p[lang="es"]]

### --feedback--

Consider the selector that targets elements with a specific [lang] attribute value.

---

[p[lang*="en"]]

### --feedback--

考虑一下针对具有特定 [lang] 属性值的元素的选择器。

## --video-solution--

1

## --text--

您将如何定位所有具有 [data-lang] 属性设置为 [fr] 的 [div] 元素？

## --answers--

[div[data-lang]]

### --feedback--

想想与 [data-lang] 属性的精确值相匹配的选择器。

---

[div[data-lang="fr"]]

---

[div[lang="fr"]]

### --feedback--

想想与 [data-lang] 属性的精确值相匹配的选择器。

---

[div[data-lang*="fr"]]

### --feedback--

想想与 [data-lang] 属性的精确值相匹配的选择器。

## --video-solution--

2

## --text--

在 HTML 中，您会使用哪个属性来指定元素内内容的语言？

## --answers--

[data-lang]

### --feedback--

此属性通常用于定义语言，以达到可访问性和搜索引擎优化的目的。

---

[class]

### --feedback--

该属性通常用于定义语言，以达到可访问性和搜索引擎优化的目的。

---

[lang]

---

[id]

### --feedback--

此属性通常用于定义语言，以达到可访问性和搜索引擎优化的目的。

## --video-solution--

3