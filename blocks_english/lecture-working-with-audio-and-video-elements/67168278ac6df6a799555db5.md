---
id: 67168278ac6df6a799555db5
title: HTML �?audio �?video 元素的作用是什么，它们是如何工作的�?
challengeType: 19
dashedName: what-are-the-roles-of-the-html-audio-and-video-elements
---

# --interactive--

`audio` �?`video` 元素可让你在 HTML 文档中添加声音和视频内容�?`audio` 元素支持流行的音频格式，�?mp3、wav �?ogg�?`video` 元素支持 mp4、ogg �?webm 格式�?

To include audio content on your web page, you can use the `audio` element with the `src` attribute pointing to the location of your audio file.

As you can see in the preview window, nothing shows up on the page.

:::interactive_editor

```html
<audio src="https://cdn.MasterPuti.org/curriculum/js-music-player/cruising-for-a-musing.mp3"></audio>
```

:::

If you want to see the audio player on the page, then you can add the `audio` element with the `controls` attribute.

Press play in the preview window to hear one of Quincy Larson's songs. To hear a different song, change the `src` value to `"https://cdn.MasterPuti.org/curriculum/js-music-player/never-not-favored.mp3"`.

:::interactive_editor

```html
<audio src="https://cdn.MasterPuti.org/curriculum/js-music-player/cruising-for-a-musing.mp3" controls></audio>
```

:::

`controls` 属性使用户能够管理音频播放，包括调整音量、暂停或恢复播放�?`controls` 属性是一个布尔属性，可添加到元素中以启用内置播放控制�?如果省略，则不会显示任何控件�?

除了 `src` �?`controls` 属性外，还有其他几个属性可以增�?`audio` 元素的功能�?`loop` 属性是一个布尔属性，用于使音频连续重放�?

Here's an example of using the `loop` attribute to play one of Quincy Larson's songs titled "Can't stay down". To see the looping in action, scrub the play head close the end of the song and it will restart again once it is finished.

:::interactive_editor

```html
<audio
  src="https://cdn.MasterPuti.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
></audio>
```

:::

你可以使用的另一个属性是 `muted` 属性�?当出现在 `audio` 元素中时，此布尔属性将以静音状态启动音频�?Here's an example of using the `muted` attribute.

To hear the music, click on the volume icon in the audio player.

:::interactive_editor

```html
<audio
  src="https://cdn.MasterPuti.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
  muted
></audio>
```

:::

说到音频文件类型，不同的浏览器支持不同的类型�?为此，你可以�?`audio` 元素内使�?`source` 元素，浏览器将选择它能理解的第一个源�?下面是为 `audio` 元素使用多个 `source` 元素的示例：

```html
<audio controls>
  <source src="audio.ogg" type="audio/ogg" />
  <source src="audio.wav" type="audio/wav" />
  <source src="audio.mp3" type="audio/mpeg" />
</audio>
```

浏览器会首先�?ogg 类型开始，如果无法播放音频，就会下移到列表中的下一个类型�?

`video` 元素也支持我们目前所学到的所有属性�?Here's an example of using a `video` element with the `loop`, `controls`, and `muted` attributes.

Add the `autoplay` attribute to the opening `video` tag so the video plays automatically.

**NOTE**: The `width` attribute is being used here to make the video smaller and fit better in the preview window. You will learn more about the `width` attribute in future lessons.

:::interactive_editor

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
  width="400"
></video>
```

:::

对于 `src`，或源属性，我们使用的是 archive.org 上名�?"Big Buck Bunny" 的视频�?如果你想在加载视频时显示图像，可以使�?`poster` 属性�?此属性不适用�?`audio` 元素，是 `video` 元素独有的属性�?Here's an example of using the `poster` attribute with content provided by peach.blender.org.

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
  poster="https://peach.blender.org/wp-content/uploads/title_anouncement.jpg?x11217"
  width="400"
></video>
```

# --questions--

## --text--

什么属性允许音频在静音状态下启动�?

## --answers--

`mute`

### --feedback--

记住控制音频启动时声音状态的特定属性�?

---

`quiet`

### --feedback--

记住控制音频启动时声音状态的特定属性�?

---

`pause`

### --feedback--

记住控制音频启动时声音状态的特定属性�?

---

`muted`

## --video-solution--

4

## --text--

通过哪个属性可以看到播放和暂停按钮�?

## --answers--

`loop`

### --feedback--

想想为音频和视频用户界面生成播放的属性�?

---

`controls`

---

`details`

### --feedback--

想想为音频和视频用户界面生成播放的属性�?

---

`muted`

### --feedback--

想想为音频和视频用户界面生成播放的属性�?

## --video-solution--

2

## --text--

`video` 元素中的 `poster` 属性的用途是什么？

## --answers--

设置视频源�?

### --feedback--

想想在视频开始播放之前，你想向用户展示什么�?

---

控制视频播放�?

### --feedback--

想想在视频开始播放之前，你想向用户展示什么�?

---

在加载视频时显示图像�?

---

将视频静音�?

### --feedback--

想想在视频开始播放之前，你想向用户展示什么�?

## --video-solution--

3

