---
id: 6733d8203da84a08a0f5eab4
title: 有哪些不同类型的视频和音频格式？
challengeType: 19
dashedName: what-are-the-different-types-of-video-and-audio-formats
---

# --description--

让我们来学习不同类型的音频和视频格式。

你可能听说过，甚至使用过一些常见的格式。如果你熟悉 MP3、MP4、MOV 或 WebM 这些术语，这些都是视频和音频的不同格式。还有相当多的其他格式可供使用。

在深入了解文件格式之前，我们需要谈谈 MIME 类型。MIME 类型代表多用途互联网邮件扩展，是一种标准化的编程方式来指示文件类型。

几乎每种文件格式都有一个 MIME 类型。例如，HTML 的类型是 `text/html`。JSON 对象的类型是 `application/json`。甚至 Windows 的 `.exe` 安装程序也有 MIME 类型：`application/vnd.microsoft.portable-executable`。

MIME 类型可以告诉应用程序（如你的浏览器）如何处理特定文件。对于音频和视频，MIME 类型表明它是一个可以嵌入网页的多媒体格式。

MP3 文件的 MIME 类型是 `audio/mp3`。然而，MP4 可以有 MIME 类型 `audio/mp4` 或 `video/mp4`，取决于它是视频文件还是纯音频文件。这种区别告诉浏览器如何处理文件。

还有许多其他文件格式，如波形格式 WAV、多用途的 OGG、Windows 媒体播放器的 WMV、开源的 MKV 等等。

了解这些文件格式之间的差异可以帮助你确保用户获得最佳体验，但有时你无法知道用户的计算机支持（或不支持）什么格式。幸运的是，`video` 和 `audio` 元素都支持 `source` 元素。

使用 `source` 元素，你可以指定文件类型和 `source`，并通过使用多个 `source` 元素包含多种不同类型。当你这样做时，浏览器将确定最适合用户当前环境的格式。

这消除了猜测工作，让你可以专注于构建引人入胜的应用程序。

# --questions--

## --text--

在文件类型的上下文中，MIME 代表什么？

## --answers--

多媒体互联网媒体扩展。

### --feedback--

MIME 类型最初是为邮件设计的。

---

多个互联网邮件扩展。

### --feedback--

MIME 类型最初是为邮件设计的。

---

多用途互联网邮件扩展。

---

媒体信息和元数据编码。

### --feedback--

MIME 类型最初是为邮件设计的。

## --video-solution--

3

## --text--

MIME 类型的主要用途是什么？

## --answers--

压缩文件大小以实现更快的互联网传输。

### --feedback--

MIME 类型指示文件的类型。

---

加密文件以实现安全传输。

### --feedback--

MIME 类型指示文件的类型。

---

标准化文件类型编程指示的方式。

---

在不同格式之间转换文件

### --feedback--

MIME 类型指示文件的类型。

## --video-solution--

3

## --text--

MP3 文件的 MIME 类型是什么？

## --answers--

`audio/mpeg`

### --feedback--

MP3 文件不能包含视频。

---

`audio/mp3`

---

`video/mp3`

### --feedback--

MP3 文件不能包含视频。

---

`application/mp3`

### --feedback--

MP3 文件不能包含视频。

## --video-solution--

2