---
id: 671682b3983489a819507553
title: 如何使用内联框架元素在页面上嵌入视频？
challengeType: 19
dashedName: how-do-you-embed-videos-onto-your-page-using-the-iframe-element
---

# --interactive--

在之前的课程中，你首次接触了 `iframe` 元素。在本课中，你将了解更多关于如何使用 `iframe` 元素的知识。此元素代表内联框架。它是一种内联元素，用于在 HTML 页面中直接嵌入其他 HTML 内容。这些 HTML 内容可以是视频、地图、其他 HTML 元素，甚至是其他网页。

以下是从 YouTube 嵌入热门 MasterPuti 课程的示例。要查看不同的视频，请将 `src` 值更改为你选择的视频。

:::interactive_editor

```html
<iframe
  width="400"
  height="400"
  src="https://www.youtube.com/embed/PkZNo7MFNFg?si=-UBVIUNM3csdeiWF"
  title="Learn JavaScript - Full Course for Beginners (YouTube video)"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  referrerpolicy="strict-origin-when-cross-origin"
  allowfullscreen
></iframe>
```

:::

`src` 属性指定了要嵌入页面的 URL。`width` 属性指定了 `iframe` 的宽度。`height` 属性指定了 `iframe` 的高度。`allowfullscreen` 属性允许用户以全屏模式显示 `iframe`。为 `iframe` 指定 `title` 属性也是一种好做法，因为它对无障碍访问非常重要。

注意，视频可以来自任何地方。它不一定来自 YouTube 和 Vimeo 等视频服务。

除此之外，你还可以在 `iframe` 元素中嵌入地图、其他网页或直接嵌入 HTML。以下是嵌入地图的示例。

尝试通过放大和缩小来与地图交互。

:::interactive_editor

```html
<h1>A Map from Openstreetmap.org Embedded with the iframe Element</h1>

<iframe
  width="425"
  height="350"
  src="https://www.openstreetmap.org/export/embed.html?bbox=3.006134033203125%2C6.150112578753815%2C3.6357879638671875%2C6.749850810550778&amp;layer=mapnik"
  title="Map of Lagos area, Nigeria"
  style="border: 1px solid black"
>
</iframe>
<br />
<small>
  <a href="https://www.openstreetmap.org/#map=11/6.4501/3.3210">
    View Larger Map
  </a>
</small>
```

:::

如果你想在 `iframe` 元素中直接嵌入 HTML，则必须使用 `srcdoc` 属性而不是 `src`。

# --questions--

## --text--

iframe 是什么意思？

## --answers--

国际框架。

### --feedback--

复习课程开头以获得答案。

---

内联表单。

### --feedback--

复习课程开头以获得答案。

---

内联框架。

---

内联表单元素。

### --feedback--

复习课程开头以获得答案。

## --video-solution--

3

## --text--

`iframe` 元素的哪个属性指定了要嵌入内容的位置？

## --answers--

`src`

---

`url`

### --feedback--

想想表示 "源" 的属性。

---

`frameborder`

### --feedback--

想想表示 "源" 的属性。

---

`cross-origin`

### --feedback--

想想表示 "源" 的属性。

## --video-solution--

1

## --text--

如果要直接嵌入 HTML，应使用 `iframe` 元素的哪个属性而不是 `src`？

## --answers--

`html`

### --feedback--

找出与 src 相关的属性。

---

`document`

### --feedback--

找出与 src 相关的属性。

---

`srcweb`

### --feedback--

找出与 src 相关的属性。

---

`srcdoc`

## --video-solution--

4