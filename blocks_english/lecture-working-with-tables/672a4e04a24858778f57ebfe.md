---
id: 672a4e04a24858778f57ebfe
title: HTML 表格是用来做什么的，又不应该用来做什么？
challengeType: 19
dashedName: what-are-html-tables-used-for
---

# --interactive--

如今，HTML 表格已经不像以前那么常用了�?但是，作为一名前端开发人员，你还是应该熟悉它们�?�?1990 年代，表格是开发者在浏览器中展示数据的最早方式之一�?

以下这段用于生成表格的示例代码来自美�?劳工统计局�?

:::interactive_editor

```html
<table id="quickfacts">
  <thead>
    <tr>
      <th colspan="2">Quick Facts: Software Developers, Quality Assurance Analysts, and Testers</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>2023 Median Pay</th>
      <td>
        $130,160 per year
        <br>$62.58 per hour
      </td>
    </tr>
    <tr>
      <th>Typical Entry-Level Education</th>
      <td>Bachelor's degree</td>
    </tr>
    <tr>
      <th>Work Experience in a Related Occupation</th>
      <td>None</td>
    </tr>
    <tr>
      <th>On-the-job Training</th>
      <td>None</td>
    </tr>
    <tr>
      <th>Number of Jobs, 2022</th>
      <td>1,795,300</td>
    </tr>
    <tr>
      <th>Job Outlook, 2022-32</th>
      <td>25% (Much faster than average)</td>
    </tr>
    <tr>
      <th>Employment Change, 2022-32</th>
      <td>451,200</td>
    </tr>
  </tbody>
  <tfoot>
    <tr>
      <th>If this table had a footer it would be here.</th>
    </tr>
  </tfoot>
</table>
```

:::

如你所见，这个表格的主元素�?`table`，设置了一�?`id` �?`quickfacts`�?表格内部包括表头元素 `thead`、表体元�?`tbody` 和表脚元�?`tfoot`�?

表头、表身和表脚元素均可包含一定数量的表格�?`tr`�?每一行表格都可以包含一个表头单元格 `th` 来标注该行数据的意义�?也可以包含若干个数据单元�?`td`，称为表格数据�?

这些 HTML 元素看起来很多�?但不要被吓倒——它们遵循着一个简单的层级结构�?

下面是一个我们可以创建的包含所有这些元素的最简单表格：

:::interactive_editor

```html
<table>
  <thead>
    <tr>
      <th>The title of this table</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>First Row</th>
      <td>
        First Data Cell
      </td>
    </tr>
    <tr>
      <th>Second Row</th>
      <td>
        Second Data Cell
      </td>
    </tr>
  </tbody>
  <tfoot>
    <tr>
      <th>The footer of this table, which might contain date of publication, author credits, or other meta information.</th>
    </tr>
  </tfoot>
</table>
```

:::

如你所见，数据始终位于 `tr` �?—�?而在 `tr` 元素中会有一�?`th` 元素作为标题单元格，以及一�?`td` 元素作为数据单元格�?

有些网站会使�?`div` 来构建自己的表格，而不是使用更合适的 `table` 元素�?

虽然使用通用�?`div` 元素也可以实现类似的布局，但使用 `table` 元素仍然是更好的选择�?

很多年前，开发人员可能会使用 `table` 来排布网页上的非数据元素�?这从未被视为最佳实践�?但在某些代码库中，你可能仍会遇到这样使用表格的情况�?

如今，开发人员使�?CSS flexbox 和网格来布局设计�?MasterPuti 稍后将深入介绍这些工具�?

现在，只需�?HTML 表格用于其最初的目的：展示表格数据�?

# --questions--

## --text--

�?HTML 表格中，`tr` 元素包含哪些元素�?

## --answers--

`thead` �?`tfoot`�?

### --feedback--

Refer back to the beginning of the lesson where the `tr` element was discussed.

---

`td` �?`th`�?

---

`table` �?`tbody`�?

### --feedback--

Refer back to the beginning of the lesson where the `tr` element was discussed.

---

`div` �?`span`�?

### --feedback--

Refer back to the beginning of the lesson where the `tr` element was discussed.

## --video-solution--

2

## --text--

以下哪些�?HTML 表格的主要部分？

## --answers--

`table`、`div` �?`span`�?

### --feedback--

Refer back to the beginning of the lesson where these elements were discussed.

---

`header`、`section` �?`footer`�?

### --feedback--

Refer back to the beginning of the lesson where these elements were discussed.

---

`thead`、`tbody` �?`tfoot`�?

---

`article`、`aside` �?`nav`�?

### --feedback--

Refer back to the beginning of the lesson where these elements were discussed.

## --video-solution--

3

## --text--

HTML 表格如今最推荐的主要用途是什么？

## --answers--

创建页面布局�?

### --feedback--

Refer back to the end of the lesson where the recommended use was discussed.

---

展示表格型数据�?

---

CSS 样式设计�?

### --feedback--

Refer back to the end of the lesson where the recommended use was discussed.

---

嵌入图片�?

### --feedback--

Refer back to the end of the lesson where the recommended use was discussed.

## --video-solution--

2

