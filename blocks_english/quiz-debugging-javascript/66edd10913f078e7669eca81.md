---
id: 66edd10913f078e7669eca81
title: JavaScript调试测验
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

当变量在定义之前被使用时会发生什么类型的错误？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

哪个JavaScript语句可以用来有意暂停代码执行以进行调试？

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

哪个控制台方法可以用来将数据显示为表格？

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

如何在Chrome开发者工具中检查网络请求？

#### --distractors--

通过使用"Network Links"选项卡。

---

通过使用"Console"选项卡。

---

通过使用"Sources"选项卡。

#### --answer--

通过使用"Network"选项卡。

### --question--

#### --text--

在JavaScript中使用`try...catch`的正确语法是什么？

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

以下哪一个是`SyntaxError`的例子？

#### --distractors--

在声明之前使用变量。

---

在同一作用域中使用变量两次。

---

使用超出作用域的变量。

#### --answer--

数组元素之间缺少逗号。

### --question--

#### --text--

JavaScript调试中断点的目的是什么？

#### --distractors--

自动纠正代码错误。

---

防止代码执行。

---

向控制台记录消息。

#### --answer--

在特定点暂停执行。

### --question--

#### --text--

当执行`throw`语句时会发生什么？

#### --distractors--

程序立即终止，忽略所有剩余代码。

---

throw语句向控制台记录错误但不停止执行。

---

暂停代码执行直到通过禁用断点恢复。

#### --answer--

抛出异常并执行`catch`块（如果存在）。

### --question--

#### --text--

在开发者工具中调试期间，你会使用哪种方法来检查变量或表达式？

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

执行以下代码时会发生什么类型的错误？

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Stack overflow error

#### --answer--

`ReferenceError`

### --question--

#### --text--

调试时通常使用哪种工具逐行执行代码？

#### --distractors--

`console.log()`

---

`alert()`

---

`throw` statement.

#### --answer--

Developer Tools中的断点。

### --question--

#### --text--

尝试访问`undefined`的属性时会发生什么类型的错误？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

`try...catch`语句中的`finally`块有什么作用？

#### --distractors--

仅在有错误时执行。

---

如果触发`catch`则跳过执行。

---

立即退出代码块。

#### --answer--

始终执行，无论是否有错误。

### --question--

#### --text--

运行以下代码的结果是什么？

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

代码将抛出`SyntaxError`。

---

代码将在控制台记录一个空数组。

---

代码将抛出内存不足错误。

#### --answer--

代码将抛出`RangeError`。

### --question--

#### --text--

关于JavaScript中的`debugger`语句，以下哪个答案是正确的？

#### --distractors--

浏览器总是忽略`debugger`语句。

---

`debugger`语句向服务器发送错误报告。

---

`debugger`语句总是重新加载页面并清除控制台。

#### --answer--

当开发者工具未打开时，`debugger`语句被忽略。

### --question--

#### --text--

JavaScript中"性能分析"的主要目的是什么？

#### --distractors--

为JavaScript函数编写单元测试并创建代码执行配置文件。

---

暂停代码执行并释放捕获的资源如内存和CPU。

---

将JavaScript代码转换为机器代码以实现更快的执行。

#### --answer--

通过记录CPU使用率、函数调用和执行时间来识别性能瓶颈。

### --question--

#### --text--

在调试的上下文中，"监视器"是什么？

#### --distractors--

暂停代码执行的工具。

---

记录错误的内置函数。

---

处理异常的JavaScript方法。

#### --answer--

监控变量值的工具。

### --question--

#### --text--

什么时候应该使用`console.error()`而不是`console.log()`？

#### --distractors--

显示表格数据。

---

仅在调试期间记录消息。

---

记录有关浏览器的信息。

#### --answer--

记录错误消息。

### --question--

#### --text--

JavaScript中的`Error()`构造函数有什么作用？

#### --distractors--

向控制台记录错误消息。

---

在沙箱中运行代码。

---

终止程序。

#### --answer--

创建可以抛出的新错误对象。

### --question--

#### --text--

JavaScript中`console.dir()`方法的用途是什么？

#### --distractors--

为日志文件创建单独的目录以处理个别错误类型。

---

生成控制台输出的可视化表示并保存在pdf文件中。

---

停止程序执行并显示与内存和CPU使用相关的错误消息。

#### --answer--

输出指定JavaScript对象属性的分层交互式列表。