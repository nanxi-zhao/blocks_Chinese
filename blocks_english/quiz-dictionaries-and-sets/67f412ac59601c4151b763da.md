---
id: 67f412ac59601c4151b763da
title: 字典和集合测验
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是字典？

#### --distractors--

只保存字符串和列表的数据结构

---

只接受大数字的键值对集合的数据结构

---

只保存元组集合的数据结构

#### --answer--

键值对集合的数据结构

### --question--

#### --text--

以下哪项是字典的正确语法？

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

以下关于字典的描述哪项是正确的？

#### --distractors--

键的长度必须至少为两个字符

---

键必须包含特殊符号

---

键必须包含至少一个数字

#### --answer--

键必须是唯一的

### --question--

#### --text--

以下哪个构造函数可以用来创建字典？

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

以下哪项是从字典中访问值的正确方法？

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

以下哪项是更新字典中值的正确方法？

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

以下哪个方法可以用来获取与键关联的值？

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

什么是视图对象？

#### --distractors--

一种将字符串转换为字典的特殊对象

---

一种仅当字典有两个或更多键值对时才能查看字典内容的方法

---

一种将列表转换为字典的特殊对象

#### --answer--

一种在不创建数据单独副本的情况下查看字典内容的方法

### --question--

#### --text--

以下哪个方法返回包含字典中所有键值对的视图对象？

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

以下哪个方法从字典中删除所有键值对？

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

下面代码的输出是什么？

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

以下关于集合的描述哪项是正确的？

#### --distractors--

集合是可变的且按逆序排列

---

集合是可变的且有序

---

集合是不可变的且无序

#### --answer--

集合是可变的且无序

### --question--

#### --text--

以下哪项是创建集合的正确方法？

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

以下哪个函数用于创建空集合？

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

终端将输出什么？

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

以下哪个方法检查集合是否为子集？

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

以下哪个方法检查两个集合是否不相交？

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

对称差集运算符(`^`)做什么？

#### --distractors--

返回一个仅包含集合共同元素的新集合

---

找出集合之间的差集并用该结果更新第一个集合

---

返回一个包含第一个集合中不在另一个集合中的元素的新集合

#### --answer--

返回一个包含在第一个或第二个集合中但不同时在两个集合中的元素的新集合

### --question--

#### --text--

以下哪个内置模块用于生成随机数？

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

以下哪个模块用于处理正则表达式？

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`