---
id: 66edcc779993c0da6906dbb9
title: JavaScript函数测验
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一项是在JavaScript中声明函数的正确方式？

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

当你调用（或执行）一个函数时会发生什么？

#### --distractors--

函数内部的代码将运行并且总是返回`null`。

---

调用函数时什么都不会发生。

---

将抛出`TypeError`并且程序会崩溃。

#### --answer--

函数内部的代码将运行并且会返回一个值。

### --question--

#### --text--

以下哪一项是调用函数的正确方式？

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

`return`关键字的作用是什么？

#### --distractors--

`return`关键字将执行函数。

---

`return`关键字将抛出类型错误。

---

`return`关键字在函数中没有任何作用。

#### --answer--

`return`关键字从函数中返回一个值。

### --question--

#### --text--

以下哪一项是有效的函数表达式？

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

什么是函数参数？

#### --distractors--

表示缺失值的值。

---

类似于匿名函数的特殊值。

---

在函数内部使用的占位符值。

#### --answer--

调用函数时传递给函数的真实值。

### --question--

#### --text--

什么是函数形参？

#### --distractors--

在函数内部使用的浮点值。

---

调用函数时传递的值。

---

表示有意缺失值的值。

#### --answer--

在函数定义中列出的值。

### --question--

#### --text--

控制台将记录什么？

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

以下哪一项是箭头函数的正确语法？

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

以下代码的结果是什么？

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

什么都不会发生，代码将正常运行。

---

字符串`"Hello there!"`将被记录到控制台。

---

字符串`"Hello!"`将被记录到控制台。

#### --answer--

你会得到一个引用错误，因为`developer`没有在全局定义。

### --question--

#### --text--

什么时候可以在箭头函数的参数列表周围省略括号？

#### --distractors--

你应该总是省略参数列表周围的括号。

---

你永远不应该省略箭头函数的括号。

---

只有在处理多个参数时才应该省略括号。

#### --answer--

只有在只有一个参数时才应该省略括号。

### --question--

#### --text--

什么时候可以省略箭头函数的大括号和`return`关键字？

#### --distractors--

你应该总是省略大括号和`return`关键字。

---

你应该永远不会省略大括号和`return`关键字。

---

当函数体有多行代码时。

#### --answer--

当函数体由单个表达式组成时。

### --question--

#### --text--

以下代码的结果是什么？

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};

exampleFunction();
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

以下哪一项是使用默认参数的正确方式？

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

以下代码的结果是什么？

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

字符串`"Hello, developer"`将被记录到控制台。

---

控制台中将显示错误消息。

---

控制台中不会显示任何内容。

#### --answer--

字符串`"Hello, Jessica"`将被记录到控制台。

### --question--

#### --text--

如果你试图删除普通函数的大括号会发生什么？

#### --distractors--

页面上将显示带有错误的警报框。

---

程序将崩溃并且无法重新启动。

---

什么都不会发生。

#### --answer--

你会得到一个语法错误。

### --question--

#### --text--

以下代码的结果是什么？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

控制台中将显示错误消息。

---

数字`0`将被记录到控制台。

---

控制台中不会记录任何内容。

#### --answer--

数字`10`将被记录到控制台。

### --question--

#### --text--

以下代码的输出结果是什么？

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

以下代码的结果是什么？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

显示错误消息说`num1`未定义。

---

数字`0`将显示在控制台中。

---

`undefined`将显示在控制台中。

#### --answer--

显示错误消息说`num2`未定义。

### --question--

#### --text--

以下代码的输出结果是什么？

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`