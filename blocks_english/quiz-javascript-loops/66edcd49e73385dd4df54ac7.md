---
id: 66edcd49e73385dd4df54ac7
title: JavaScript循环测验
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项最恰当地描述了迭代？

#### --distractors--

这是重复编写代码直到正确工作的过程。

---

这是一种将数据存储在连续内存位置中的技术。

---

这是在组件之间发送信号以建立连接的过程。

#### --answer--

这是重复执行一组指令多次的过程。

### --question--

#### --text--

编程中使用什么技术进行迭代？

#### --distractors--

递归

---

条件语句

---

编译

#### --answer--

循环

### --question--

#### --text--

以下哪项是可迭代对象？

#### --distractors--

未定义

---

数字

---

布尔值

#### --answer--

字符串

### --question--

#### --text--

以下哪项不是JavaScript支持的循环类型？

#### --distractors--

`for`循环。

---

`while`循环。

---

`for...in`循环。

#### --answer--

`if...else`循环。

### --question--

#### --text--

`for`循环声明的正确顺序是什么？

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

哪种循环在检查条件是否为真之前执行一次代码块，并且只要条件保持为真就会继续工作？

#### --distractors--

`while`循环。

---

`for...of`循环。

---

`for...in`循环。

#### --answer--

`do...while`循环。

### --question--

#### --text--

以下哪项将遍历可迭代对象的值？

#### --distractors--

`for...in`循环。

---

`for`循环。

---

`do...while`循环。

#### --answer--

`for...of`循环。

### --question--

#### --text--

以下哪项最恰当地定义了无限循环？

#### --distractors--

条件变为假时停止运行的循环。

---

运行一次然后终止的循环。

---

在固定次数迭代后自动停止的循环。

#### --answer--

终止条件永远不会满足或不存在的循环。

### --question--

#### --text--

以下循环将运行多少次？

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

哪种循环最适合遍历对象？

#### --distractors--

`for...of`循环。

---

`do...while`循环。

---

`for`循环。

#### --answer--

`for...in`循环。

### --question--

#### --text--

`for...in`循环和`for...of`循环之间有什么区别？

#### --distractors--

`for...in`循环遍历属性值，而`for...of`循环遍历属性名（键）。

---

`for...in`循环创建新属性，而`for...of`循环修改现有属性。

---

`for...in`循环只能与字符串一起使用，而`for...of`循环可以与字符串和数字一起使用。

#### --answer--

`for...in`循环遍历属性名（键），而`for...of`循环遍历属性值。

### --question--

#### --text--

以下哪项会导致无限循环？

#### --distractors--

变量初始化。

---

最终返回假的循环条件。

---

增量/减量逻辑。

#### --answer--

始终返回真的循环条件。

### --question--

#### --text--

可以使用什么语句完全跳出循环？

#### --distractors--

`continue`语句。

---

`label`语句。

---

`skip`语句。

#### --answer--

`break`语句。

### --question--

#### --text--

可以使用哪个语句跳过当前迭代并移动到下一次迭代？

#### --distractors--

`break`语句。

---

`label`语句。

---

`return`语句。

#### --answer--

`continue`语句。

### --question--

#### --text--

以下代码在控制台中打印什么？

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

以下代码会在控制台中打印什么？

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

以下代码会在控制台中打印什么？

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

以下代码会在控制台中打印什么？

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

以下代码会在控制台中打印什么？

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

以下代码会输出字符串`in the loop`多少次？

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3次

---

4次

---

5次

#### --answer--

无限次