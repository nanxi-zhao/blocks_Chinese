---
id: 66edc3ab8c6413c344f401bf
title: JavaScript数学测验
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

`NaN`代表什么？

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

以下哪项不是JavaScript中的算术运算符？

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

如果在JavaScript中尝试除以零会发生什么？

#### --distractors--

JavaScript将输出0。

---

什么都不会发生。

---

JavaScript将抛出一个错误。

#### --answer--

JavaScript将输出`Infinity`

### --question--

#### --text--

以下哪项是指数运算符的正确使用？

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

在JavaScript中递减变量时应该使用哪个运算符？

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

以下哪项不是按位运算符？

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

以下哪项是获取余数的正确复合运算符？

#### --distractors--

`//=` 

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

`==`和`===`运算符之间有什么区别？

#### --distractors--

没有区别。比较结果将始终相同。

---

`==`运算符用于检查数据类型是否相同，而`===`用于检查值是否相等。

---

`==`运算符只能用于原始类型，而`===`运算符仅用于对象。

#### --answer--

`==`运算符只比较值，而`===`运算符比较值和类型。

### --question--

#### --text--

一元加法运算符（`+`）用来做什么？

#### --distractors--

用于将值递减一。

---

用于从操作中获取余数。

---

用于反转数字的二进制表示。

#### --answer--

用于将其操作数转换为数字。

### --question--

#### --text--

以下哪个`Math`方法将值四舍五入到最近的整数？

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

`Math.sqrt()`方法做什么？

#### --distractors--

此方法返回一个随机浮点数。

---

此方法返回一个数字的绝对值。

---

此方法返回一个数字的立方。

#### --answer--

此方法返回一个数字的平方根。

### --question--

#### --text--

以下哪项不是`Math`对象的有效方法？

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

以下哪项将返回`false`？

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

`parseFloat()`方法做什么？

#### --distractors--

它解析字符串参数并对它执行按位操作。

---

它解析字符串参数并返回`false`。

---

它解析字符串参数并返回一个整数。

#### --answer--

它解析字符串参数并返回一个浮点数。

### --question--

#### --text--

控制台将记录什么？

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

以下代码的输出是什么？

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

什么是运算符优先级？

#### --distractors--

运算符优先级用于检查左侧的值是否小于右侧的值。

---

运算符优先级用于将变量提升到指定数字的幂并将结果重新赋值给变量。

---

运算符优先级用于将值增加一。

#### --answer--

运算符优先级确定表达式中操作的评估顺序。

### --question--

#### --text--

以下哪项不是比较运算符？

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

一元否定运算符（`-`）做什么？

#### --distractors--

它翻转其操作数的布尔值。

---

它将值减少一。

---

它将值增加一。

#### --answer--

它否定操作数。

### --question--

#### --text--

以下哪项是使用空值合并运算符的示例？

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```