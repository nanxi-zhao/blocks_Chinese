---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript正则表达式测验
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项不是有效的正则表达式？

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

哪个`RegExp`包含与正则表达式`/f[o0]{2} b[a4@]r/i`相同的模式？

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

正则表达式方法`test`的返回类型是什么？

#### --distractors--

包含匹配正则表达式的字符串数组。

---

包含有关正则表达式匹配的（子）字符串信息的对象。

---

`null`，`test`方法只验证给定的正则表达式是否有效。

#### --answer--

指示字符串是否匹配正则表达式的布尔值。

### --question--

#### --text--

当传递一个没有标志的正则表达式时，`match`方法会做什么？

#### --distractors--

它搜索匹配给定正则表达式的所有字符串，并将所有匹配项作为数组返回。

---

它搜索第一个完整匹配项，并返回该匹配项的起始索引。

---

它搜索第一个完整匹配项，并返回一个布尔值，指示是否找到匹配项。

#### --answer--

它搜索第一个完整匹配项，并返回包含该第一个匹配项的数组。

### --question--

#### --text--

`test`的最佳使用场景是什么？

#### --distractors--

用于验证给定的正则表达式。

---

用于获取有关字符串匹配的详细信息，例如匹配字符串的长度。

---

用于从给定字符串中提取所有匹配项。

#### --answer--

用于检查给定字符串是否匹配正则表达式。

### --question--

#### --text--

使用`replace`的目的是什么？

#### --distractors--

用新的正则表达式替换当前正则表达式。

---

当给定索引和字符串时，用提供的字符串替换指定索引处的字符。

---

从字符串中移除所有匹配的子字符串实例。

#### --answer--

用给定的替换字符串替换匹配的字符串。

### --question--

#### --text--

`match`和`matchAll`之间有什么区别？

#### --distractors--

`match`总是返回字符串，而`matchAll`总是返回匹配项数组。

---

`match`返回一个布尔值，指示是否找到匹配项，而`matchAll`返回一个数字，表示找到的匹配项数量。

---

`match`返回一个数字，指示找到的第一个匹配项的起始索引，而`matchAll`返回一个数字数组，包含找到匹配项的索引。

#### --answer--

`match`返回匹配项字符串数组，而`matchAll`返回找到的所有匹配项的迭代器对象。

### --question--

#### --text--

以下哪项是`replaceAll`的正确使用？

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

以下哪个字符类等同于正则表达式`/[a-zA-Z0-9_]/`？

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

以下哪个字符类最适合从电话号码中提取数字？

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

在正则表达式中使用前瞻`(?=)`会发生什么？

#### --distractors--

前瞻将断言到字符串的左侧，验证子模式是否存在。

---

前瞻是在主模式无法匹配时使用的替代正则表达式模式。

---

前瞻将断言到字符串的右侧，验证子模式不存在。

#### --answer--

前瞻断言到字符串的右侧，验证子模式存在。

### --question--

#### --text--

在制作正则表达式时，后瞻`(?<=)`应该放在哪里？

#### --distractors--

后瞻可以放在正则表达式的任何位置。

---

后瞻应该放在正则表达式主模式的右侧。

---

后瞻应该包含正则表达式的主模式。

#### --answer--

后瞻应该放在正则表达式主模式的左侧。

### --question--

#### --text--

哪个量词匹配前面元素零次或一次？

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

以下哪个正则表达式只允许0到999,999之间的数字？

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

关于自定义字符类（`[]`），以下哪个陈述是正确的？

#### --distractors--

它是一组要从匹配中移除的字符。

---

它可以定义一组字符来匹配，而无需转义任何特殊字符。

---

它表示Unicode形式的字符集。

#### --answer--

它可以定义一组自定义字符来匹配。

### --question--

#### --text--

以下哪个字符类正确匹配大写字母表？

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

在正则表达式中使用捕获组`(...)`会发生什么？

#### --distractors--

捕获组会立即评估，无论它在正则表达式中的位置如何。

---

捕获组尝试使用给定的子模式进行匹配，并在不记忆结果的情况下继续。

---

捕获组是优先的子模式，当找到子模式的匹配项时会立即返回。

#### --answer--

捕获组尝试使用给定的子模式进行匹配，并在内存中保存结果后继续。

### --question--

#### --text--

在正则表达式中使用非捕获组`(?:...)`会发生什么？

#### --distractors--

非捕获组尝试使用给定的子模式进行匹配，并在内存中保存结果后继续。

---

非捕获组在找到给定子模式的匹配项时会立即终止。

---

非捕获组被认为是可选匹配，因此只有成功匹配才会添加到结果中。

#### --answer--

非捕获组尝试使用给定的子模式进行匹配，并在不将结果保存在内存中的情况下继续。

### --question--

#### --text--

给定以下正则表达式，这些测试中的哪一个将返回`true`？

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

通配符字符类（`.`）在正则表达式中做什么？

#### --distractors--

它只匹配空白字符。

---

它匹配字符串的开头。

---

它匹配单个数字。

#### --answer--

它匹配除换行符外的任何单个字符。