---
id: 68376949899fcbad3c9cac23
title: JavaScript和无障碍性测验
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪个属性用于无障碍性目的，以指示控件是展开还是折叠？

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

哪两个属性可以与`aria-expanded`结合使用，以在控制元素和受其控制的元素之间建立编程连接？

#### --distractors--

`aria-labelledBy`和`aria-owns`

---

`aria-controls`和`aria-label`

---

`aria-live`和`aria-owns`

#### --answer--

`aria-controls`和`aria-owns`

### --question--

#### --text--

以下哪个属性在页面上创建一个实时区域，可用于在发生时通知屏幕阅读器用户实时区域中的动态内容更改？

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

以下哪个`div`元素只会对使用辅助技术（如屏幕阅读器）的人禁用？

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

以下哪项是`aria-expanded`属性的有效使用？

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

哪个属性用于在页面上创建控制元素和受影响元素之间的编程关系？

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

以下哪项不是实时区域的常见用例？

#### --distractors--

操作后显示的消息（如错误消息或确认）。

---

定期更新的内容（如跑马灯、滚动条或倒计时器）。

---

一般状态消息（如关于进程的更新）。

#### --answer--

操作前显示的消息（如确认对话框）。

### --question--

#### --text--

以下哪个示例是正确指示哪个选项卡当前被选中的方式？

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

以下哪项不是`aria-live`属性的有效值？

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

以下哪个`aria-live`属性值意味着更新非常重要且具有最高优先级？

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`