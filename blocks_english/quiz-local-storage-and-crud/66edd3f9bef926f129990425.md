---
id: 66edd3f9bef926f129990425
title: 本地存储和CRUD测验
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

在Web开发的背景下，CRUD代表什么？

#### --distractors--

创建、替换、撤销、删除。

---

连接、移除、更新、显示。

---

复制、检索、更新、删除。

#### --answer--

创建、读取、更新、删除。

### --question--

#### --text--

在RESTful API中，哪个HTTP方法通常与创建操作相关联？

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

在数据库中修改现有记录时，通常会执行以下哪个CRUD操作？

#### --distractors--

读取

---

删除

---

创建

#### --answer--

更新

### --question--

#### --text--

Web Storage API中localStorage和sessionStorage的主要区别是什么？

#### --distractors--

localStorage将数据存储在内存中，而sessionStorage将数据存储在磁盘上。

---

sessionStorage比localStorage检索数据更快。

---

localStorage数据仅在会话期间可用，而sessionStorage数据在会话间持久化。

#### --answer--

localStorage数据会持久化直到显式删除，而sessionStorage数据在页面会话结束时被清除。

### --question--

#### --text--

使用哪个方法在localStorage中存储数据？

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

如何从sessionStorage中移除单个项？

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

`PUT`和`PATCH`之间有什么区别？

#### --distractors--

`PUT`修改特定字段，而`PATCH`替换整个资源。

---

`PUT`只添加新资源，而`PATCH`更新现有资源。

---

`PUT`用于检索数据，而`PATCH`用于发送数据。

#### --answer--

`PUT`替换整个资源，而`PATCH`更新特定字段。

### --question--

#### --text--

缓存存储是用来做什么的？

#### --distractors--

缓存存储用于在浏览器中安全地永久存储用户凭证以进行身份验证。

---

缓存存储用于在浏览器中使用数据库系统存储大量结构化数据。

---

缓存存储用于通过存储跨多个网站可访问的键值对来跟踪用户会话和偏好。

#### --answer--

缓存存储用于存储经常请求的网络资源，减少加载时间并提高性能。

### --question--

#### --text--

在Web开发的背景下，cookie是什么？

#### --distractors--

一种HTTP请求类型。

---

在浏览器中运行的一段代码。

---

在服务器上存储大文件的方法。

#### --answer--

由用户Web浏览器存储的一小段数据。

### --question--

#### --text--

以下哪个方法用于在JavaScript中手动设置cookie？

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

以下哪个代码片段正确地删除了名为`username`的cookie？

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

关于cookie，以下哪个陈述是错误的？

#### --distractors--

会话cookie在浏览器关闭时过期。

---

HttpOnly cookie无法通过JavaScript访问。

---

安全cookie仅通过HTTPS传输。

#### --answer--

Cookie只能由服务器设置。

### --question--

#### --text--

什么是浏览器指纹识别？

#### --distractors--

一种基于用户IP地址识别用户的技术。

---

一种数据加密形式。

---

一种在浏览器缓存中存储数据的方法。

#### --answer--

一种通过浏览器和设备属性跟踪用户的方式。

### --question--

#### --text--

在本地存储中存储密码的主要风险是什么？

#### --distractors--

本地存储数据在关闭浏览器标签页后会被删除。

---

本地存储数据在删除cookie后会丢失。

---

本地存储无法离线工作。

#### --answer--

本地存储未加密且容易被访问。

### --question--

#### --text--

关于客户端存储，以下哪个陈述是正确的？

#### --distractors--

客户端存储使用对象而不是字符串来存储数据。

---

客户端存储使用云服务在多个设备间同步用户数据。

---

客户端存储比服务器端存储更安全。

#### --answer--

客户端存储允许网站在用户设备上存储数据。

### --question--

#### --text--

与localStorage相比，IndexedDB的主要优势是什么？

#### --distractors--

IndexedDB不需要用户同意即可存储数据。

---

IndexedDB有更简单的API。

---

IndexedDB在所有Web浏览器中都受支持。

#### --answer--

IndexedDB可以处理更大量的数据和更复杂的查询。

### --question--

#### --text--

以下哪项是从IndexedDB中的对象存储检索数据的有效方式？

#### --distractors--

在对象存储上调用`retrieve()`。

---

使用`query()`访问特定记录。

---

使用`fetch()`方法。

#### --answer--

在存储引用上使用`get()`。

### --question--

#### --text--

IndexedDB支持什么类型的事务？

#### --distractors--

只读事务。

---

异步事务。

---

同步事务。

#### --answer--

读写事务。

### --question--

#### --text--

关于Service Worker的生命周期，以下哪个陈述是正确的？

#### --distractors--

Service Worker在Web应用程序的主线程中运行。

---

Service Worker可以在激活之前安装。

---

Service Worker只能在Web应用程序在浏览器中打开时运行。

#### --answer--

Service Worker在激活后可以拦截网络请求。

### --question--

#### --text--

以下哪个陈述最好地描述了渐进式Web应用程序(PWA)的关键特性？

#### --distractors--

PWA只能安装在移动设备上。

---

PWA需要后端服务器。

---

PWA只能通过特定浏览器访问。

#### --answer--

PWA通过Service Worker提供离线功能。