---
id: 66f1af82732957c895f0b21a
title: SQL 和 PostgreSQL 测验
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是关系型数据库？

#### --distractors--

一种结构化数据库，用于识别存储项目之间的非关系。

---

存储非结构化数据的数据库。

---

基于平面文件的系统。

#### --answer--

一种结构化数据库，用于识别和管理表中数据之间的关系。

### --question--

#### --text--

非关系型数据库与关系型数据库有何不同？

#### --distractors--

非关系型数据库使用表，而关系型数据库不使用。

---

关系型数据库可以存储非结构化数据。

---

非关系型数据库比关系型数据库慢。

#### --answer--

非关系型数据库可以存储非结构化数据。

### --question--

#### --text--

在 PostgreSQL 交互式终端（`psql`）中执行 `\l` 命令会做什么？

#### --distractors--

列出所有模式。

---

列出所有表。

---

连接到数据库。

#### --answer--

列出所有数据库。

### --question--

#### --text--

以下哪个命令在 SQL 中创建新表？

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

外键在关系型数据库中的作用是什么？

#### --distractors--

允许数据重复。

---

唯一标识表中的行。

---

存储大量文本数据。

#### --answer--

在两个表之间创建关系。

### --question--

#### --text--

哪个命令会向现有表添加新列？

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

当表 A 中的一条记录对应表 B 中的一条或多条记录时，存在什么类型的关系？

#### --distractors--

一对一 (1-1)

---

多对多 (m-m)

---

多对一 (m-1)

#### --answer--

一对多 (1-m)

### --question--

#### --text--

什么是查询？

#### --distractors--

从数据库中删除所有数据的请求。

---

数据库中用于唯一值的特殊行。

---

数据库中用于唯一值的特殊列。

#### --answer--

从数据库中请求特定数据。

### --question--

#### --text--

如何在 SQL 中从表中删除列？

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

SQL 中 `PRIMARY KEY` 约束的功能是什么？

#### --distractors--

链接两个表。

---

定义列的默认值。

---

允许列中存在空值。

#### --answer--

确保列包含唯一值。

### --question--

#### --text--

`\c` 命令在 PostgreSQL 交互式终端（`psql`）中做什么？

#### --distractors--

列出当前数据库中的所有表。

---

显示当前数据库连接详细信息。

---

退出 `psql` shell。

#### --answer--

连接到指定数据库。

### --question--

#### --text--

什么是关系型数据库中的复合键？

#### --distractors--

从另一个键派生的键。

---

根据数据更新频繁变化的键。

---

专门用于外键关系的键。

#### --answer--

由多个列组成的键，用于唯一标识记录。

### --question--

#### --text--

哪个命令将数据插入表中？

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

如何在 SQL 中重命名列？

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

哪个查询将删除 employees 表中的所有记录？

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

以下哪个 SQL 约束专门确保列只能包含唯一值，允许空值？

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

以下哪项不是行业中常用的常见关系型数据库示例？

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

一对一 (1-1) 关系在关系型数据库中表示什么？

#### --distractors--

表 A 中的每条记录对应表 B 中的一条或多条记录。

---

表 A 中的每条记录对应表 B 中没有记录。

---

表 A 中的每条记录对应表 B 中的多条记录，反之亦然。

#### --answer--

表 A 中的每条记录恰好对应表 B 中的一条记录。

### --question--

#### --text--

哪个命令将列出当前 PostgreSQL 数据库中的所有表？

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

以下哪项允许你将表与自身连接？

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`