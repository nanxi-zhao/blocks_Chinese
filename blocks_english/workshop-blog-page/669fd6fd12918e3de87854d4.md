---
id: 669fd6fd12918e3de87854d4
title: 步骤 8
challengeType: 0
dashedName: step-8
---

# --description--

用户应该有办法浏览你的博客页面上的不同部分�?

`nav` 元素用于提供指向文档中其他部分或网站中其他部分的导航链接�?你经常会看到用于菜单或目录的 `nav` 元素�?

下面是使�?`nav` 元素的示例：

```html
<nav>
  <ul>
    <li><a href="#">Home</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>
```

�?`figure` 元素下方，添加一�?`nav` 元素，在其中嵌套一�?`ul` 元素�?

�?`ul` 元素中，添加三个 `li` 元素�?

# --hints--

你的 `nav` 元素应有一个开始标签�?

```js
assert.match(code, /<nav\>/);
```

你的 `nav` 元素应该有一个结束标签�?

```js
assert.match(code, /<\/nav\>/);
```

你的 `nav` 元素应该�?`header` 元素中�?

```js
const navElement = document.querySelector('nav');
assert.strictEqual(navElement?.parentElement.tagName, 'HEADER');
```

你的 `ul` 元素应该有一个开始标签�?

```js
assert.match(code, /<ul\>/);
```

你的 `ul` 元素应该有一个结束标签�?

```js
assert.match(code, /<\/ul\>/);
```

你的 `ul` 元素应在 `nav` 元素中�?

```js
const ulElement = document.querySelector('ul');
assert.strictEqual(ulElement?.parentElement.tagName, 'NAV');
```

你的 `ul` 元素应包含三�?`li` 元素�?

```js
const ulElement = document.querySelector("ul");

const liElements = ulElement?.querySelectorAll("li");
assert.strictEqual(liElements?.length, 3);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Mr. Whiskers' Blog</title>
    <meta charset="UTF-8" />
  </head>
  <body>
    <header>
      <h1>Welcome to Mr. Whiskers' Blog Page!</h1>
      <figure>
        <img
          src="https://cdn.MasterPuti.org/curriculum/css-photo-gallery/1.jpg"
          alt="a cat in the garden"
        />
        <figcaption>Mr. Whiskers in the Garden</figcaption>
      </figure>
--fcc-editable-region--

--fcc-editable-region--
    </header>
  </body>
</html>
```

