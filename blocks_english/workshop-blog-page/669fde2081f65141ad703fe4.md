---
id: 669fde2081f65141ad703fe4
title: 步骤 11
challengeType: 0
dashedName: step-11
---

# --description--

页面的第一个部分将是关于部分�?该部分将介绍 Whiskers 先生，让用户了解本博客的内容�?

在你�?`main` 元素中，添加一�?`section` 元素，其 `id` 属性设置为 `"about"`�?

�?`section` 元素中，添加一�?`h2`，其文本�?`About`�?

# --hints--

你的 `section` 元素应该有一个开始标签�?

```js
assert.match(code, /<section.*>/);
```

你的 `section` 元素应该有一个结束标签�?

```js
assert.match(code, /<\/section\>/);
```

你的 `section` 元素应该有一�?`id`，设置为 `"about"`�?

```js
const sectionElement = document.querySelector('section');
assert.strictEqual(sectionElement?.getAttribute("id"), "about");
```

你的 `section` 元素应该�?`main` 元素中�?

```js
const sectionElement = document.getElementById('about');
assert.strictEqual(sectionElement?.parentElement.tagName, 'MAIN');
```

你的 `h2` 元素应该有一个开始标签�?

```js
assert.match(code, /<h2\>/);
```

你的 `h2` 元素应该有一个结束标签�?

```js
assert.match(code, /<\/h2\>/);
```

你的 `h2` 元素的文本应�?`About`�?仔细检查你的拼写�?

```js
const h2Element = document.querySelector('body h2');
assert.strictEqual(h2Element?.innerText.trim(), "About");
```

你的 `h2` 元素应该�?`section` 元素中�?

```js
const h2Element = document.querySelector('#about h2');
assert.strictEqual(h2Element?.parentElement.tagName, 'SECTION');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Mr. Whiskers' Blog</title>
    <meta charset="UTF-8" />
  </head>
  <body>
    <header>
      <h1>Welcome to Mr. Whiskers' Blog Page!</h1>
      <figure>
        <img
          src="https://cdn.MasterPuti.org/curriculum/css-photo-gallery/1.jpg"
          alt="a cat in the garden"
        />
        <figcaption>Mr. Whiskers in the Garden</figcaption>
      </figure>
      <nav>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#posts">Posts</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </header>
--fcc-editable-region--
    <main>

    </main>
--fcc-editable-region--
  </body>
</html>
```

