---
id: 669fdfc9a5e3da42d2376609
title: 步骤 13
challengeType: 0
dashedName: step-13
---

# --description--

现在你已添加了关于部分，请尝试点�?`About` 链接，查看页面是否能跳转到该部分�?

博客页面的下一部分将是有关 Whiskers 先生的博文列表�?

添加另一�?`section` 元素，其 `id` 设置�?`"posts"`�?

�?`section` 元素中，添加一�?`h2` 元素，其文本�?`Posts`�?

# --hints--

你的代码应该有一�?`section` 开始标签�?

```js
assert.match(code, /<section.*>/i);
```

你的代码应该有一�?`section` 结束标签�?

```js
assert.match(code, /<\/section>/i);
```

你的 `section` 元素应该有一�?`id`，设置为 `"posts"`�?

```js
assert.match(code, /<section.*id="posts".*>/i);
```

你的 `section` 元素应该�?`main` 元素中�?

```js
const sectionElement = document.getElementById('posts');
assert.strictEqual(sectionElement?.parentElement.tagName, 'MAIN');
```

你的 `h2` 元素应该�?`section` 元素中�?

```js
const h2Element = document.querySelector('#posts h2');
assert.strictEqual(h2Element?.parentElement.tagName, 'SECTION');
```

你的 `h2` 元素的文本应�?`Posts`�?

```js
const h2Element = document.querySelector('#posts h2');
assert.strictEqual(h2Element?.innerText.trim(), 'Posts');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Mr. Whiskers' Blog</title>
    <meta charset="UTF-8" />
  </head>
  <body>
    <header>
      <h1>Welcome to Mr. Whiskers' Blog Page!</h1>
      <figure>
        <img
          src="https://cdn.MasterPuti.org/curriculum/css-photo-gallery/1.jpg"
          alt="a cat in the garden"
        />
        <figcaption>Mr. Whiskers in the Garden</figcaption>
      </figure>
      <nav>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#posts">Posts</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </header>
    <main>
      <section id="about">
        <h2>About</h2>
        <p>
          Hi there! I'm Jane Doe, a passionate writer who finds endless inspiration in the antics of my beloved cat, Mr. Whiskers.
        </p>
        <p>
          His playful nature and boundless energy keeps me on my toes. I love him so much.
        </p>
      </section>
      --fcc-editable-region--

      --fcc-editable-region--
    </main>
  </body>
</html>
```

