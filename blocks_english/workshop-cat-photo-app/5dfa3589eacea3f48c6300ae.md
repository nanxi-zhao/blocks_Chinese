---
id: 5dfa3589eacea3f48c6300ae
title: 步骤 20
challengeType: 0
dashedName: step-20
---

# --description--

在第二个 `section` 元素中，添加一个带有文�?`Cat Lists` 的新 `h2` 元素�?

# --hints--

你的 `section` 元素应该有一个开始标签�?开始标签的语法为：`<elementName>`�?

```js
assert.lengthOf(document.querySelectorAll('section'), 2);
assert.lengthOf(code.match(/<\/section>/g), 2);
```

你的 `h2` 元素应该有一个开始标签�?开始标签的语法为：`<elementName>`�?

```js
assert.lengthOf(document.querySelectorAll('h2'), 2);
```

你的 `h2` 元素应该有一个结束标签�?结束标签�?`<` 字符之后有一�?`/`�?

```js
assert.lengthOf(code.match(/<\/h2\>/g), 2);
```

你的第二�?`h2` 元素应该在第二个 `section` 元素的结束标签的上方�?它的位置不对�?

```js
const secondSection = document.querySelectorAll('section')[1];
assert.equal(secondSection?.lastElementChild?.nodeName, 'H2');
```

第二�?`h2` 元素应该有文�?`Cat Lists`�?你要么遗漏了文本，要么拼写有误�?

```js
assert.equal(
  document
    .querySelectorAll('main > section')[1]
    ?.lastElementChild?.innerText?.trim().replace(/\s+/g, ' ').toLowerCase(), 'cat lists'
);
```

# --seed--

## --seed-contents--

```html
<html>
  <body>
    <main>
      <h1>CatPhotoApp</h1>
      <section>
        <h2>Cat Photos</h2>
        <p>Everyone loves <a href="https://cdn.MasterPuti.org/curriculum/cat-photo-app/running-cats.jpg">cute cats</a> online!</p>
        <p>See more <a target="_blank" href="https://freecatphotoapp.com">cat photos</a> in our gallery.</p>
        <a href="https://freecatphotoapp.com"><img src="https://cdn.MasterPuti.org/curriculum/cat-photo-app/relaxing-cat.jpg" alt="A cute orange cat lying on its back."></a>
      </section>
--fcc-editable-region--
      <section>

      </section>
--fcc-editable-region--
    </main>
  </body>
</html>
```

