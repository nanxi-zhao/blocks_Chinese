---
id: 641da3c6b6fbd742bff6ee40
title: 步骤 2
challengeType: 0
dashedName: step-2
---

# --description--

<dfn>Fetch API</dfn> 是一个内置的 JavaScript 接口，用于向服务器发起网络请求。它有一个 `fetch()` 方法，你可以用它来发起 `GET`、`POST`、`PUT` 或 `PATCH` 请求。在这个项目中，你将向一个 JSON 文件的 URL 发起 `GET` 请求，该文件包含了 MasterPuti 新闻作者的信息。

下面是你如何使用 `fetch()` 方法发起 `GET` 请求：

```js
fetch("url-goes-here")
```

向这个 URL 发起 `GET` 请求：`"https://cdn.MasterPuti.org/curriculum/news-author-page/authors.json"`。先不要在代码末尾加分号。

# --before-all--

```js
window.fetch = () => Promise.resolve({json: () => Promise.resolve([{ author: 'Whoever', image: 'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==', url: "http://not-a-real-url.nowhere/", bio: 'words go here' }])});
```

# --hints--

你应该使用 `fetch()` 方法向 `"https://cdn.MasterPuti.org/curriculum/news-author-page/authors.json"` 发起 `GET` 请求。

```js
assert.match(code, /fetch\(\s*('|"|`)https:\/\/cdn\.MasterPuti\.org\/curriculum\/news\-author\-page\/authors\.json\1\s*\)/)
```

你不应该在 fetch 调用后加分号。

```js
assert.notMatch(code, /fetch\(\s*('|"|`)https:\/\/cdn\.MasterPuti\.org\/curriculum\/news\-author\-page\/authors\.json\1\s*\)\s*;/)
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MasterPuti 新闻作者页面</title>
    <link rel="stylesheet" href="./styles.css" />
  </head>
  <body>
    <h1 class="title">MasterPuti 新闻作者页面</h1>

    <main>
      <div id="author-container"></div>
      <button class="btn" id="load-more-btn">加载更多作者</button>
    </main>

    <script src="./script.js"></script>
  </body>
</html>
```

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --main-bg-color: #1b1b32;
  --light-grey: #f5f6f7;
  --dark-purple: #5a01a7;
  --golden-yellow: #feac32;
}

body {
  background-color: var(--main-bg-color);
  text-align: center;
}

.title {
  color: var(--light-grey);
  margin: 20px 0;
}

#author-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}

.user-card {
  border-radius: 15px;
  width: 300px;
  height: 350px;
  background-color: var(--light-grey);
  margin: 20px;
}

.user-img {
  width: 150px;
  height: 150px;
  object-fit: cover;
}

.purple-divider {
  background-color: var(--dark-purple);
  width: 100%;
  height: 15px;
}

.author-name {
  margin: 10px;
}

.bio {
  margin: 20px;
}

.error-msg {
  color: var(--light-grey);
}

.btn {
  cursor: pointer;
  width: 200px;
  margin: 10px;
  color: var(--main-bg-color);
  font-size: 14px;
  background-color: var(--golden-yellow);
  background-image: linear-gradient(#fecc4c, #ffac33);
  border-color: var(--golden-yellow);
  border-width: 3px;
}
```

```js
const authorContainer = document.getElementById('author-container');
const loadMoreBtn = document.getElementById('load-more-btn');

--fcc-editable-region--

--fcc-editable-region--
```