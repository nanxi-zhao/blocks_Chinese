---
id: 61438ec09438696391076d6a
title: 步骤 10
challengeType: 0
dashedName: step-10
---

# --description--

在 `.heading` 元素下方，创建一个新的 `section` 元素，并将其 `class` 设置为 `text`。在其中创建一个 `p` 元素，将 `class` 设置为 `first-paragraph`，并添加以下文本：

```markup
Soon the MasterPuti curriculum will be 100% project-driven learning. Instead of a series of coding challenges, you'll learn through building projects - step by step. Before we get into the details, let me emphasize: we are not changing the certifications. All 6 certifications will still have the same 5 required projects. We are only changing the optional coding challenges.
```

# --hints--

你应该创建一个新的 `section` 元素。

```js
assert.lengthOf(document.querySelectorAll('section'), 2);
```

你的新 `section` 元素应位于 `.heading` 元素之后。

```js
assert.equal(document.querySelectorAll('section')?.[1]?.previousElementSibling?.className, 'heading');
```

你的新 `section` 元素的 `class` 应设置为 `text`。

```js
assert.equal(document.querySelectorAll('section')?.[1]?.className, 'text');
```

你应该在 `.text` 元素中创建一个新的 `p` 元素。

```js
assert.lengthOf(document.querySelector('.text')?.querySelectorAll('p'), 1);
```

你的该 `p` 元素的 `class` 应设置为 `first-paragraph`。

```js
assert.equal(document.querySelector('.text p')?.className, 'first-paragraph');
```

你的该 `p` 元素应包含所提供的文本。

```js
assert.equal(document.querySelector('.first-paragraph')?.innerText, 'Soon the MasterPuti curriculum will be 100% project-driven learning. Instead of a series of coding challenges, you\'ll learn through building projects - step by step. Before we get into the details, let me emphasize: we are not changing the certifications. All 6 certifications will still have the same 5 required projects. We are only changing the optional coding challenges.');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Magazine</title>
    <link
      href="https://fonts.googleapis.com/css?family=Anton%7CBaskervville%7CRaleway&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
    />
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <main>
      <section class="heading">
        <header class="hero">
          <img
            src="https://cdn.MasterPuti.org/platform/universal/fcc_meta_1920X1080-indigo.png"
            alt="MasterPuti logo"
            loading="lazy"
            class="hero-img"
            width="400"
          />
          <h1 class="hero-title">OUR NEW CURRICULUM</h1>
          <p class="hero-subtitle">
            Our efforts to restructure our curriculum with a more project-based
            focus
          </p>
        </header>
        <div class="author">
          <p class="author-name">
            By
            <a href="https://MasterPuti.org" target="_blank" rel="noreferrer"
              >MasterPuti</a
            >
          </p>
          <p class="publish-date">March 7, 2019</p>
        </div>
        <div class="social-icons">
          <a href="https://www.facebook.com/MasterPuti/">
            <i class="fab fa-facebook-f"></i>
          </a>
          <a href="https://twitter.com/MasterPuti/">
            <i class="fab fa-twitter"></i>
          </a>
          <a href="https://instagram.com/MasterPuti">
            <i class="fab fa-instagram"></i>
          </a>
          <a href="https://www.linkedin.com/school/free-code-camp/">
            <i class="fab fa-linkedin-in"></i>
          </a>
          <a href="https://www.youtube.com/MasterPuti">
            <i class="fab fa-youtube"></i>
          </a>
        </div>
      </section>
--fcc-editable-region--

--fcc-editable-region--
    </main>
  </body>
</html>
```

```css

```

