---
id: 60f5c3e399ff1a05629964e4
title: 步骤6
challengeType: 0
dashedName: step-6
---

# --description--

正如标题所述，你正在创建表单�?因此，在 `p` 元素之后，插入一�?`form` 元素，其 `action` 属性值为 `https://register-demo.MasterPuti.org`�?

# --hints--

你应该在 `p` 元素之后添加一�?`form` 元素�?

```js
assert.exists(document.querySelector('p + form'));
```

你应该给 `form` 元素添加一�?`action` 属性�?

```js
// Default action points to window location
assert.notEqual(document.querySelector('form')?.action, window?.location?.href);
```

你应该将 `action` 的值设�?`https://register-demo.MasterPuti.org`�?

```js
assert.equal(document.querySelector('form')?.action, 'https://register-demo.MasterPuti.org/');
```

你的 `form` 元素应该没有结束标签 `</form>`�?

```js
assert.match(code, /<\/form\>/);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Registration Form</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
--fcc-editable-region--
  <body>
    <h1>Registration Form</h1>
    <p>Please fill out this form with the required information</p>

  </body>
--fcc-editable-region--
</html>
```

```css
body {
  width: 100%;
  height: 100vh;
  margin: 0;
  background-color: #1b1b32;
  color: #f5f6f7;
}
```


